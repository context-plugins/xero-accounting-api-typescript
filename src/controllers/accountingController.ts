/**
 * Xero Accounting APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, FileWrapper, RequestOptions } from '../core.js';
import { Account, accountSchema } from '../models/account.js';
import { Accounts, accountsSchema } from '../models/accounts.js';
import { Actions, actionsSchema } from '../models/actions.js';
import { Allocations, allocationsSchema } from '../models/allocations.js';
import { Attachments, attachmentsSchema } from '../models/attachments.js';
import {
  BankTransactions,
  bankTransactionsSchema,
} from '../models/bankTransactions.js';
import { BankTransfers, bankTransfersSchema } from '../models/bankTransfers.js';
import { BatchPayments, batchPaymentsSchema } from '../models/batchPayments.js';
import {
  BrandingThemes,
  brandingThemesSchema,
} from '../models/brandingThemes.js';
import {
  CISOrgSettings,
  cISOrgSettingsSchema,
} from '../models/cISOrgSettings.js';
import { CISSettings, cISSettingsSchema } from '../models/cISSettings.js';
import { ContactGroups, contactGroupsSchema } from '../models/contactGroups.js';
import { Contacts, contactsSchema } from '../models/contacts.js';
import { CreditNotes, creditNotesSchema } from '../models/creditNotes.js';
import { Currencies, currenciesSchema } from '../models/currencies.js';
import { Currency, currencySchema } from '../models/currency.js';
import { Employees, employeesSchema } from '../models/employees.js';
import { ExpenseClaims, expenseClaimsSchema } from '../models/expenseClaims.js';
import {
  HistoryRecords,
  historyRecordsSchema,
} from '../models/historyRecords.js';
import {
  ImportSummaryObject,
  importSummaryObjectSchema,
} from '../models/importSummaryObject.js';
import {
  InvoiceReminders,
  invoiceRemindersSchema,
} from '../models/invoiceReminders.js';
import { Invoices, invoicesSchema } from '../models/invoices.js';
import { Items, itemsSchema } from '../models/items.js';
import { Journals, journalsSchema } from '../models/journals.js';
import {
  LinkedTransaction,
  linkedTransactionSchema,
} from '../models/linkedTransaction.js';
import {
  LinkedTransactions,
  linkedTransactionsSchema,
} from '../models/linkedTransactions.js';
import {
  ManualJournals,
  manualJournalsSchema,
} from '../models/manualJournals.js';
import {
  OnlineInvoices,
  onlineInvoicesSchema,
} from '../models/onlineInvoices.js';
import { Organisations, organisationsSchema } from '../models/organisations.js';
import { Overpayments, overpaymentsSchema } from '../models/overpayments.js';
import { Payment, paymentSchema } from '../models/payment.js';
import { PaymentDelete, paymentDeleteSchema } from '../models/paymentDelete.js';
import { Payments, paymentsSchema } from '../models/payments.js';
import {
  PaymentService,
  paymentServiceSchema,
} from '../models/paymentService.js';
import {
  PaymentServices,
  paymentServicesSchema,
} from '../models/paymentServices.js';
import { Prepayments, prepaymentsSchema } from '../models/prepayments.js';
import {
  PurchaseOrders,
  purchaseOrdersSchema,
} from '../models/purchaseOrders.js';
import { Quotes, quotesSchema } from '../models/quotes.js';
import { Receipts, receiptsSchema } from '../models/receipts.js';
import {
  RepeatingInvoices,
  repeatingInvoicesSchema,
} from '../models/repeatingInvoices.js';
import { Reports, reportsSchema } from '../models/reports.js';
import {
  ReportWithRows,
  reportWithRowsSchema,
} from '../models/reportWithRows.js';
import { RequestEmpty, requestEmptySchema } from '../models/requestEmpty.js';
import { Setup, setupSchema } from '../models/setup.js';
import { Status20Enum, status20EnumSchema } from '../models/status20Enum.js';
import { TaxRates, taxRatesSchema } from '../models/taxRates.js';
import { TimeframeEnum, timeframeEnumSchema } from '../models/timeframeEnum.js';
import {
  TrackingCategories,
  trackingCategoriesSchema,
} from '../models/trackingCategories.js';
import {
  TrackingCategory,
  trackingCategorySchema,
} from '../models/trackingCategory.js';
import {
  TrackingOption,
  trackingOptionSchema,
} from '../models/trackingOption.js';
import {
  TrackingOptions,
  trackingOptionsSchema,
} from '../models/trackingOptions.js';
import { Users, usersSchema } from '../models/users.js';
import {
  array,
  boolean,
  number,
  optional,
  string,
  unknown,
} from '../schema.js';
import { BaseController } from './baseController.js';
import { CustomError } from '../errors/customError.js';

export class AccountingController extends BaseController {
  /**
   * @param xeroTenantId      Xero identifier for Tenant
   * @param ifModifiedSince   Only records created or modified since this timestamp will be returned
   * @param where             Filter by an any element
   * @param order             Order by an any element
   * @return Response from the API call
   */
  async getAccounts(
    xeroTenantId: string,
    ifModifiedSince?: string,
    where?: string,
    order?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Accounts>> {
    const req = this.createRequest('GET', '/Accounts');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      ifModifiedSince: [ifModifiedSince, optional(string())],
      where: [where, optional(string())],
      order: [order, optional(string())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('If-Modified-Since', mapped.ifModifiedSince);
    req.query('where', mapped.where);
    req.query('order', mapped.order);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(accountsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param body           Account object in body of request
   * @return Response from the API call
   */
  async createAccount(
    xeroTenantId: string,
    body: Account,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Accounts>> {
    const req = this.createRequest('PUT', '/Accounts');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      body: [body, accountSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.throwOn(
      400,
      CustomError,
      'Validation Error - some data was incorrect returns response of type Error'
    );
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(accountsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param accountID      Unique identifier for retrieving single object
   * @return Response from the API call
   */
  async deleteAccount(
    xeroTenantId: string,
    accountID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Accounts>> {
    const req = this.createRequest('DELETE');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      accountID: [accountID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/Accounts/${mapped.accountID}`;
    req.throwOn(
      400,
      CustomError,
      'Validation Error - some data was incorrect returns response of type Error'
    );
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(accountsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param accountID      Unique identifier for retrieving single object
   * @return Response from the API call
   */
  async getAccount(
    xeroTenantId: string,
    accountID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Accounts>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      accountID: [accountID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/Accounts/${mapped.accountID}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(accountsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param accountID      Unique identifier for retrieving single object
   * @param body           Request of type Accounts array with one Account
   * @return Response from the API call
   */
  async updateAccount(
    xeroTenantId: string,
    accountID: string,
    body: Accounts,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Accounts>> {
    const req = this.createRequest('POST');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      accountID: [accountID, string()],
      body: [body, accountsSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/Accounts/${mapped.accountID}`;
    req.throwOn(
      400,
      CustomError,
      'Validation Error - some data was incorrect returns response of type Error'
    );
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(accountsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param accountID      Unique identifier for Account object
   * @return Response from the API call
   */
  async getAccountAttachments(
    xeroTenantId: string,
    accountID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Attachments>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      accountID: [accountID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/Accounts/${mapped.accountID}/Attachments`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(attachmentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param accountID      Unique identifier for Account object
   * @param attachmentID   Unique identifier for Attachment object
   * @param contentType    The mime type of the attachment file you are retrieving i.e image/jpg,
   *                                 application/pdf
   * @return Response from the API call
   */
  async getAccountAttachmentById(
    xeroTenantId: string,
    accountID: string,
    attachmentID: string,
    contentType: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<NodeJS.ReadableStream | Blob>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      accountID: [accountID, string()],
      attachmentID: [attachmentID, string()],
      contentType: [contentType, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('contentType', mapped.contentType);
    req.appendTemplatePath`/Accounts/${mapped.accountID}/Attachments/${mapped.attachmentID}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsStream(requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param accountID      Unique identifier for Account object
   * @param fileName       Name of the attachment
   * @param contentType    The mime type of the attachment file you are retrieving i.e image/jpg,
   *                                 application/pdf
   * @return Response from the API call
   */
  async getAccountAttachmentByFileName(
    xeroTenantId: string,
    accountID: string,
    fileName: string,
    contentType: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<NodeJS.ReadableStream | Blob>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      accountID: [accountID, string()],
      fileName: [fileName, string()],
      contentType: [contentType, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('contentType', mapped.contentType);
    req.appendTemplatePath`/Accounts/${mapped.accountID}/Attachments/${mapped.fileName}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsStream(requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param accountID      Unique identifier for Account object
   * @param fileName       Name of the attachment
   * @param body           Byte array of file in body of request
   * @return Response from the API call
   */
  async updateAccountAttachmentByFileName(
    xeroTenantId: string,
    accountID: string,
    fileName: string,
    body: FileWrapper,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Attachments>> {
    const req = this.createRequest('POST');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      accountID: [accountID, string()],
      fileName: [fileName, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/octet-stream');
    req.formData({ body: body });
    req.appendTemplatePath`/Accounts/${mapped.accountID}/Attachments/${mapped.fileName}`;
    req.throwOn(
      400,
      CustomError,
      'Validation Error - some data was incorrect returns response of type Error'
    );
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(attachmentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param accountID      Unique identifier for Account object
   * @param fileName       Name of the attachment
   * @param body           Byte array of file in body of request
   * @return Response from the API call
   */
  async createAccountAttachmentByFileName(
    xeroTenantId: string,
    accountID: string,
    fileName: string,
    body: FileWrapper,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Attachments>> {
    const req = this.createRequest('PUT');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      accountID: [accountID, string()],
      fileName: [fileName, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/octet-stream');
    req.formData({ body: body });
    req.appendTemplatePath`/Accounts/${mapped.accountID}/Attachments/${mapped.fileName}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(attachmentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId      Xero identifier for Tenant
   * @param ifModifiedSince   Only records created or modified since this timestamp will be returned
   * @param where             Filter by an any element
   * @param order             Order by an any element
   * @param page              Up to 100 bank transactions will be returned in a single API call with line
   *                                    items details
   * @param unitdp            e.g. unitdp=4 – (Unit Decimal Places) You can opt in to use four decimal
   *                                    places for unit amounts
   * @return Response from the API call
   */
  async getBankTransactions(
    xeroTenantId: string,
    ifModifiedSince?: string,
    where?: string,
    order?: string,
    page?: number,
    unitdp?: number,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<BankTransactions>> {
    const req = this.createRequest('GET', '/BankTransactions');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      ifModifiedSince: [ifModifiedSince, optional(string())],
      where: [where, optional(string())],
      order: [order, optional(string())],
      page: [page, optional(number())],
      unitdp: [unitdp, optional(number())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('If-Modified-Since', mapped.ifModifiedSince);
    req.query('where', mapped.where);
    req.query('order', mapped.order);
    req.query('page', mapped.page);
    req.query('unitdp', mapped.unitdp);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(bankTransactionsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param body
   * @param summarizeErrors If false return 200 OK and mix of successfully created objects
   *                                                   and any with validation errors
   * @param unitdp          e.g. unitdp=4 – (Unit Decimal Places) You can opt in to use
   *                                                   four decimal places for unit amounts
   * @return Response from the API call
   */
  async updateOrCreateBankTransactions(
    xeroTenantId: string,
    body: BankTransactions,
    summarizeErrors?: boolean,
    unitdp?: number,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<BankTransactions>> {
    const req = this.createRequest('POST', '/BankTransactions');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      body: [body, bankTransactionsSchema],
      summarizeErrors: [summarizeErrors, optional(boolean())],
      unitdp: [unitdp, optional(number())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.query('summarizeErrors', mapped.summarizeErrors);
    req.query('unitdp', mapped.unitdp);
    req.json(mapped.body);
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(bankTransactionsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param body            BankTransactions with an array of BankTransaction objects in
   *                                                   body of request
   * @param summarizeErrors If false return 200 OK and mix of successfully created objects
   *                                                   and any with validation errors
   * @param unitdp          e.g. unitdp=4 – (Unit Decimal Places) You can opt in to use
   *                                                   four decimal places for unit amounts
   * @return Response from the API call
   */
  async createBankTransactions(
    xeroTenantId: string,
    body: BankTransactions,
    summarizeErrors?: boolean,
    unitdp?: number,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<BankTransactions>> {
    const req = this.createRequest('PUT', '/BankTransactions');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      body: [body, bankTransactionsSchema],
      summarizeErrors: [summarizeErrors, optional(boolean())],
      unitdp: [unitdp, optional(number())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.query('summarizeErrors', mapped.summarizeErrors);
    req.query('unitdp', mapped.unitdp);
    req.json(mapped.body);
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(bankTransactionsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId      Xero identifier for Tenant
   * @param bankTransactionID Xero generated unique identifier for a bank transaction
   * @param unitdp            e.g. unitdp=4 – (Unit Decimal Places) You can opt in to use four decimal
   *                                    places for unit amounts
   * @return Response from the API call
   */
  async getBankTransaction(
    xeroTenantId: string,
    bankTransactionID: string,
    unitdp?: number,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<BankTransactions>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      bankTransactionID: [bankTransactionID, string()],
      unitdp: [unitdp, optional(number())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.query('unitdp', mapped.unitdp);
    req.appendTemplatePath`/BankTransactions/${mapped.bankTransactionID}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(bankTransactionsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId      Xero identifier for Tenant
   * @param bankTransactionID Xero generated unique identifier for a bank transaction
   * @param body
   * @param unitdp            e.g. unitdp=4 – (Unit Decimal Places) You can opt in to use
   *                                                     four decimal places for unit amounts
   * @return Response from the API call
   */
  async updateBankTransaction(
    xeroTenantId: string,
    bankTransactionID: string,
    body: BankTransactions,
    unitdp?: number,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<BankTransactions>> {
    const req = this.createRequest('POST');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      bankTransactionID: [bankTransactionID, string()],
      body: [body, bankTransactionsSchema],
      unitdp: [unitdp, optional(number())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.query('unitdp', mapped.unitdp);
    req.json(mapped.body);
    req.appendTemplatePath`/BankTransactions/${mapped.bankTransactionID}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(bankTransactionsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId      Xero identifier for Tenant
   * @param bankTransactionID Xero generated unique identifier for a bank transaction
   * @return Response from the API call
   */
  async getBankTransactionAttachments(
    xeroTenantId: string,
    bankTransactionID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Attachments>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      bankTransactionID: [bankTransactionID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/BankTransactions/${mapped.bankTransactionID}/Attachments`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(attachmentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId      Xero identifier for Tenant
   * @param bankTransactionID Xero generated unique identifier for a bank transaction
   * @param attachmentID      Xero generated unique identifier for an attachment
   * @param contentType       The mime type of the attachment file you are retrieving i.e image/jpg,
   *                                    application/pdf
   * @return Response from the API call
   */
  async getBankTransactionAttachmentById(
    xeroTenantId: string,
    bankTransactionID: string,
    attachmentID: string,
    contentType: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<NodeJS.ReadableStream | Blob>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      bankTransactionID: [bankTransactionID, string()],
      attachmentID: [attachmentID, string()],
      contentType: [contentType, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('contentType', mapped.contentType);
    req.appendTemplatePath`/BankTransactions/${mapped.bankTransactionID}/Attachments/${mapped.attachmentID}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsStream(requestOptions);
  }

  /**
   * @param xeroTenantId      Xero identifier for Tenant
   * @param bankTransactionID Xero generated unique identifier for a bank transaction
   * @param fileName          The name of the file being attached
   * @param contentType       The mime type of the attachment file you are retrieving i.e image/jpg,
   *                                    application/pdf
   * @return Response from the API call
   */
  async getBankTransactionAttachmentByFileName(
    xeroTenantId: string,
    bankTransactionID: string,
    fileName: string,
    contentType: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<NodeJS.ReadableStream | Blob>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      bankTransactionID: [bankTransactionID, string()],
      fileName: [fileName, string()],
      contentType: [contentType, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('contentType', mapped.contentType);
    req.appendTemplatePath`/BankTransactions/${mapped.bankTransactionID}/Attachments/${mapped.fileName}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsStream(requestOptions);
  }

  /**
   * @param xeroTenantId      Xero identifier for Tenant
   * @param bankTransactionID Xero generated unique identifier for a bank transaction
   * @param fileName          The name of the file being attached
   * @param body              Byte array of file in body of request
   * @return Response from the API call
   */
  async updateBankTransactionAttachmentByFileName(
    xeroTenantId: string,
    bankTransactionID: string,
    fileName: string,
    body: FileWrapper,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Attachments>> {
    const req = this.createRequest('POST');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      bankTransactionID: [bankTransactionID, string()],
      fileName: [fileName, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/octet-stream');
    req.formData({ body: body });
    req.appendTemplatePath`/BankTransactions/${mapped.bankTransactionID}/Attachments/${mapped.fileName}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(attachmentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId      Xero identifier for Tenant
   * @param bankTransactionID Xero generated unique identifier for a bank transaction
   * @param fileName          The name of the file being attached
   * @param body              Byte array of file in body of request
   * @return Response from the API call
   */
  async createBankTransactionAttachmentByFileName(
    xeroTenantId: string,
    bankTransactionID: string,
    fileName: string,
    body: FileWrapper,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Attachments>> {
    const req = this.createRequest('PUT');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      bankTransactionID: [bankTransactionID, string()],
      fileName: [fileName, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/octet-stream');
    req.formData({ body: body });
    req.appendTemplatePath`/BankTransactions/${mapped.bankTransactionID}/Attachments/${mapped.fileName}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(attachmentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId      Xero identifier for Tenant
   * @param bankTransactionID Xero generated unique identifier for a bank transaction
   * @return Response from the API call
   */
  async getBankTransactionsHistory(
    xeroTenantId: string,
    bankTransactionID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoryRecords>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      bankTransactionID: [bankTransactionID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/BankTransactions/${mapped.bankTransactionID}/History`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(historyRecordsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId      Xero identifier for Tenant
   * @param bankTransactionID Xero generated unique identifier for a bank transaction
   * @param body              HistoryRecords containing an array of HistoryRecord objects in
   *                                                   body of request
   * @return Response from the API call
   */
  async createBankTransactionHistoryRecord(
    xeroTenantId: string,
    bankTransactionID: string,
    body: HistoryRecords,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoryRecords>> {
    const req = this.createRequest('PUT');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      bankTransactionID: [bankTransactionID, string()],
      body: [body, historyRecordsSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/BankTransactions/${mapped.bankTransactionID}/History`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(historyRecordsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId      Xero identifier for Tenant
   * @param ifModifiedSince   Only records created or modified since this timestamp will be returned
   * @param where             Filter by an any element
   * @param order             Order by an any element
   * @return Response from the API call
   */
  async getBankTransfers(
    xeroTenantId: string,
    ifModifiedSince?: string,
    where?: string,
    order?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<BankTransfers>> {
    const req = this.createRequest('GET', '/BankTransfers');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      ifModifiedSince: [ifModifiedSince, optional(string())],
      where: [where, optional(string())],
      order: [order, optional(string())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('If-Modified-Since', mapped.ifModifiedSince);
    req.query('where', mapped.where);
    req.query('order', mapped.order);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(bankTransfersSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param body           BankTransfers with array of BankTransfer objects in request body
   * @return Response from the API call
   */
  async createBankTransfer(
    xeroTenantId: string,
    body: BankTransfers,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<BankTransfers>> {
    const req = this.createRequest('PUT', '/BankTransfers');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      body: [body, bankTransfersSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(bankTransfersSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param bankTransferID Xero generated unique identifier for a bank transfer
   * @return Response from the API call
   */
  async getBankTransfer(
    xeroTenantId: string,
    bankTransferID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<BankTransfers>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      bankTransferID: [bankTransferID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/BankTransfers/${mapped.bankTransferID}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(bankTransfersSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param bankTransferID Xero generated unique identifier for a bank transfer
   * @return Response from the API call
   */
  async getBankTransferAttachments(
    xeroTenantId: string,
    bankTransferID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Attachments>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      bankTransferID: [bankTransferID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/BankTransfers/${mapped.bankTransferID}/Attachments`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(attachmentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param bankTransferID Xero generated unique identifier for a bank transfer
   * @param attachmentID   Xero generated unique identifier for an Attachment to a bank transfer
   * @param contentType    The mime type of the attachment file you are retrieving i.e image/jpg,
   *                                 application/pdf
   * @return Response from the API call
   */
  async getBankTransferAttachmentById(
    xeroTenantId: string,
    bankTransferID: string,
    attachmentID: string,
    contentType: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<NodeJS.ReadableStream | Blob>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      bankTransferID: [bankTransferID, string()],
      attachmentID: [attachmentID, string()],
      contentType: [contentType, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('contentType', mapped.contentType);
    req.appendTemplatePath`/BankTransfers/${mapped.bankTransferID}/Attachments/${mapped.attachmentID}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsStream(requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param bankTransferID Xero generated unique identifier for a bank transfer
   * @param fileName       The name of the file being attached to a Bank Transfer
   * @param contentType    The mime type of the attachment file you are retrieving i.e image/jpg,
   *                                 application/pdf
   * @return Response from the API call
   */
  async getBankTransferAttachmentByFileName(
    xeroTenantId: string,
    bankTransferID: string,
    fileName: string,
    contentType: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<NodeJS.ReadableStream | Blob>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      bankTransferID: [bankTransferID, string()],
      fileName: [fileName, string()],
      contentType: [contentType, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('contentType', mapped.contentType);
    req.appendTemplatePath`/BankTransfers/${mapped.bankTransferID}/Attachments/${mapped.fileName}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsStream(requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param bankTransferID Xero generated unique identifier for a bank transfer
   * @param fileName       The name of the file being attached to a Bank Transfer
   * @param body           Byte array of file in body of request
   * @return Response from the API call
   */
  async updateBankTransferAttachmentByFileName(
    xeroTenantId: string,
    bankTransferID: string,
    fileName: string,
    body: FileWrapper,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Attachments>> {
    const req = this.createRequest('POST');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      bankTransferID: [bankTransferID, string()],
      fileName: [fileName, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/octet-stream');
    req.formData({ body: body });
    req.appendTemplatePath`/BankTransfers/${mapped.bankTransferID}/Attachments/${mapped.fileName}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(attachmentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param bankTransferID Xero generated unique identifier for a bank transfer
   * @param fileName       The name of the file being attached to a Bank Transfer
   * @param body           Byte array of file in body of request
   * @return Response from the API call
   */
  async createBankTransferAttachmentByFileName(
    xeroTenantId: string,
    bankTransferID: string,
    fileName: string,
    body: FileWrapper,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Attachments>> {
    const req = this.createRequest('PUT');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      bankTransferID: [bankTransferID, string()],
      fileName: [fileName, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/octet-stream');
    req.formData({ body: body });
    req.appendTemplatePath`/BankTransfers/${mapped.bankTransferID}/Attachments/${mapped.fileName}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(attachmentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param bankTransferID Xero generated unique identifier for a bank transfer
   * @return Response from the API call
   */
  async getBankTransferHistory(
    xeroTenantId: string,
    bankTransferID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoryRecords>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      bankTransferID: [bankTransferID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/BankTransfers/${mapped.bankTransferID}/History`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(historyRecordsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param bankTransferID Xero generated unique identifier for a bank transfer
   * @param body           HistoryRecords containing an array of HistoryRecord objects in
   *                                                body of request
   * @return Response from the API call
   */
  async createBankTransferHistoryRecord(
    xeroTenantId: string,
    bankTransferID: string,
    body: HistoryRecords,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoryRecords>> {
    const req = this.createRequest('PUT');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      bankTransferID: [bankTransferID, string()],
      body: [body, historyRecordsSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/BankTransfers/${mapped.bankTransferID}/History`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(historyRecordsSchema, requestOptions);
  }

  /**
   * Batch payments allow you to bundle multiple bills or invoices into one payment transaction. This
   * means a single payment in Xero can be reconciled with a single transaction on the bank statement
   * making for a much simpler bank reconciliation experience.
   *
   * @param xeroTenantId      Xero identifier for Tenant
   * @param ifModifiedSince   Only records created or modified since this timestamp will be returned
   * @param where             Filter by an any element
   * @param order             Order by an any element
   * @return Response from the API call
   */
  async getBatchPayments(
    xeroTenantId: string,
    ifModifiedSince?: string,
    where?: string,
    order?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<BatchPayments>> {
    const req = this.createRequest('GET', '/BatchPayments');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      ifModifiedSince: [ifModifiedSince, optional(string())],
      where: [where, optional(string())],
      order: [order, optional(string())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('If-Modified-Since', mapped.ifModifiedSince);
    req.query('where', mapped.where);
    req.query('order', mapped.order);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(batchPaymentsSchema, requestOptions);
  }

  /**
   * Batch payments allow you to bundle multiple bills or invoices into one payment transaction. This
   * means a single payment in Xero can be reconciled with a single transaction on the bank statement
   * making for a much simpler bank reconciliation experience.
   *
   * @param xeroTenantId    Xero identifier for Tenant
   * @param body            BatchPayments with an array of Payments in body of request
   * @param summarizeErrors If false return 200 OK and mix of successfully created objects and
   *                                                any with validation errors
   * @return Response from the API call
   */
  async createBatchPayment(
    xeroTenantId: string,
    body: BatchPayments,
    summarizeErrors?: boolean,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<BatchPayments>> {
    const req = this.createRequest('PUT', '/BatchPayments');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      body: [body, batchPaymentsSchema],
      summarizeErrors: [summarizeErrors, optional(boolean())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.query('summarizeErrors', mapped.summarizeErrors);
    req.json(mapped.body);
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(batchPaymentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param batchPaymentID Unique identifier for BatchPayment
   * @return Response from the API call
   */
  async getBatchPaymentHistory(
    xeroTenantId: string,
    batchPaymentID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoryRecords>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      batchPaymentID: [batchPaymentID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/BatchPayments/${mapped.batchPaymentID}/History`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(historyRecordsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param batchPaymentID Unique identifier for BatchPayment
   * @param body           HistoryRecords containing an array of HistoryRecord objects in
   *                                                body of request
   * @return Response from the API call
   */
  async createBatchPaymentHistoryRecord(
    xeroTenantId: string,
    batchPaymentID: string,
    body: HistoryRecords,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoryRecords>> {
    const req = this.createRequest('PUT');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      batchPaymentID: [batchPaymentID, string()],
      body: [body, historyRecordsSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/BatchPayments/${mapped.batchPaymentID}/History`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(historyRecordsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @return Response from the API call
   */
  async getBrandingThemes(
    xeroTenantId: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<BrandingThemes>> {
    const req = this.createRequest('GET', '/BrandingThemes');
    const mapped = req.prepareArgs({ xeroTenantId: [xeroTenantId, string()] });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(brandingThemesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param brandingThemeID Unique identifier for a Branding Theme
   * @return Response from the API call
   */
  async getBrandingTheme(
    xeroTenantId: string,
    brandingThemeID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<BrandingThemes>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      brandingThemeID: [brandingThemeID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/BrandingThemes/${mapped.brandingThemeID}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(brandingThemesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param brandingThemeID Unique identifier for a Branding Theme
   * @return Response from the API call
   */
  async getBrandingThemePaymentServices(
    xeroTenantId: string,
    brandingThemeID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<PaymentServices>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      brandingThemeID: [brandingThemeID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/BrandingThemes/${mapped.brandingThemeID}/PaymentServices`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(paymentServicesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param brandingThemeID Unique identifier for a Branding Theme
   * @param body            PaymentService object in body of request
   * @return Response from the API call
   */
  async createBrandingThemePaymentServices(
    xeroTenantId: string,
    brandingThemeID: string,
    body: PaymentService,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<PaymentServices>> {
    const req = this.createRequest('POST');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      brandingThemeID: [brandingThemeID, string()],
      body: [body, paymentServiceSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/BrandingThemes/${mapped.brandingThemeID}/PaymentServices`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(paymentServicesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param where          Filter by an any element
   * @param order          Order by an any element
   * @return Response from the API call
   */
  async getContactGroups(
    xeroTenantId: string,
    where?: string,
    order?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ContactGroups>> {
    const req = this.createRequest('GET', '/ContactGroups');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      where: [where, optional(string())],
      order: [order, optional(string())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.query('where', mapped.where);
    req.query('order', mapped.order);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(contactGroupsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param body           ContactGroups with an array of names in request body
   * @return Response from the API call
   */
  async createContactGroup(
    xeroTenantId: string,
    body: ContactGroups,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ContactGroups>> {
    const req = this.createRequest('PUT', '/ContactGroups');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      body: [body, contactGroupsSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.throwOn(
      400,
      CustomError,
      'Validation Error - some data was incorrect returns response of type Error'
    );
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(contactGroupsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param contactGroupID Unique identifier for a Contact Group
   * @return Response from the API call
   */
  async getContactGroup(
    xeroTenantId: string,
    contactGroupID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ContactGroups>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      contactGroupID: [contactGroupID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/ContactGroups/${mapped.contactGroupID}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(contactGroupsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param contactGroupID Unique identifier for a Contact Group
   * @param body           an array of Contact groups with Name of specific group to update
   * @return Response from the API call
   */
  async updateContactGroup(
    xeroTenantId: string,
    contactGroupID: string,
    body: ContactGroups,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ContactGroups>> {
    const req = this.createRequest('POST');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      contactGroupID: [contactGroupID, string()],
      body: [body, contactGroupsSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/ContactGroups/${mapped.contactGroupID}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(contactGroupsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param contactGroupID Unique identifier for a Contact Group
   * @return Response from the API call
   */
  async deleteContactGroupContacts(
    xeroTenantId: string,
    contactGroupID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<void>> {
    const req = this.createRequest('DELETE');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      contactGroupID: [contactGroupID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/ContactGroups/${mapped.contactGroupID}/Contacts`;
    req.authenticate([{ oAuth2: true }]);
    return req.call(requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param contactGroupID Unique identifier for a Contact Group
   * @param body           Contacts with array of contacts specifying the ContactID to be added to
   *                                          ContactGroup in body of request
   * @return Response from the API call
   */
  async createContactGroupContacts(
    xeroTenantId: string,
    contactGroupID: string,
    body: Contacts,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Contacts>> {
    const req = this.createRequest('PUT');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      contactGroupID: [contactGroupID, string()],
      body: [body, contactsSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/ContactGroups/${mapped.contactGroupID}/Contacts`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(contactsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param contactGroupID Unique identifier for a Contact Group
   * @param contactID      Unique identifier for a Contact
   * @return Response from the API call
   */
  async deleteContactGroupContact(
    xeroTenantId: string,
    contactGroupID: string,
    contactID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<void>> {
    const req = this.createRequest('DELETE');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      contactGroupID: [contactGroupID, string()],
      contactID: [contactID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/ContactGroups/${mapped.contactGroupID}/Contacts/${mapped.contactID}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.call(requestOptions);
  }

  /**
   * @param xeroTenantId      Xero identifier for Tenant
   * @param ifModifiedSince   Only records created or modified since this timestamp will be returned
   * @param where             Filter by an any element
   * @param order             Order by an any element
   * @param iDs               Filter by a comma separated list of ContactIDs. Allows you to retrieve a
   *                                      specific set of contacts in a single call.
   * @param page              e.g. page=1 - Up to 100 contacts will be returned in a single API call.
   * @param includeArchived   e.g. includeArchived=true - Contacts with a status of ARCHIVED will be
   *                                      included in the response
   * @return Response from the API call
   */
  async getContacts(
    xeroTenantId: string,
    ifModifiedSince?: string,
    where?: string,
    order?: string,
    iDs?: string[],
    page?: number,
    includeArchived?: boolean,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Contacts>> {
    const req = this.createRequest('GET', '/Contacts');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      ifModifiedSince: [ifModifiedSince, optional(string())],
      where: [where, optional(string())],
      order: [order, optional(string())],
      iDs: [iDs, optional(array(string()))],
      page: [page, optional(number())],
      includeArchived: [includeArchived, optional(boolean())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('If-Modified-Since', mapped.ifModifiedSince);
    req.query('where', mapped.where);
    req.query('order', mapped.order);
    req.query('IDs', mapped.iDs);
    req.query('page', mapped.page);
    req.query('includeArchived', mapped.includeArchived);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(contactsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param body
   * @param summarizeErrors If false return 200 OK and mix of successfully created objects and any
   *                                           with validation errors
   * @return Response from the API call
   */
  async updateOrCreateContacts(
    xeroTenantId: string,
    body: Contacts,
    summarizeErrors?: boolean,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Contacts>> {
    const req = this.createRequest('POST', '/Contacts');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      body: [body, contactsSchema],
      summarizeErrors: [summarizeErrors, optional(boolean())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.query('summarizeErrors', mapped.summarizeErrors);
    req.json(mapped.body);
    req.throwOn(
      400,
      CustomError,
      'Validation Error - some data was incorrect returns response of type Error'
    );
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(contactsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param body            Contacts with an array of Contact objects to create in body of request
   * @param summarizeErrors If false return 200 OK and mix of successfully created objects and any
   *                                           with validation errors
   * @return Response from the API call
   */
  async createContacts(
    xeroTenantId: string,
    body: Contacts,
    summarizeErrors?: boolean,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Contacts>> {
    const req = this.createRequest('PUT', '/Contacts');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      body: [body, contactsSchema],
      summarizeErrors: [summarizeErrors, optional(boolean())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.query('summarizeErrors', mapped.summarizeErrors);
    req.json(mapped.body);
    req.throwOn(
      400,
      CustomError,
      'Validation Error - some data was incorrect returns response of type Error'
    );
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(contactsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param contactID      Unique identifier for a Contact
   * @return Response from the API call
   */
  async getContact(
    xeroTenantId: string,
    contactID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Contacts>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      contactID: [contactID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/Contacts/${mapped.contactID}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(contactsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param contactID      Unique identifier for a Contact
   * @param body           an array of Contacts containing single Contact object with properties to
   *                                          update
   * @return Response from the API call
   */
  async updateContact(
    xeroTenantId: string,
    contactID: string,
    body: Contacts,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Contacts>> {
    const req = this.createRequest('POST');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      contactID: [contactID, string()],
      body: [body, contactsSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/Contacts/${mapped.contactID}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(contactsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param contactID      Unique identifier for a Contact
   * @return Response from the API call
   */
  async getContactAttachments(
    xeroTenantId: string,
    contactID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Attachments>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      contactID: [contactID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/Contacts/${mapped.contactID}/Attachments`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(attachmentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param contactID      Unique identifier for a Contact
   * @param attachmentID   Unique identifier for a Attachment
   * @param contentType    The mime type of the attachment file you are retrieving i.e image/jpg,
   *                                 application/pdf
   * @return Response from the API call
   */
  async getContactAttachmentById(
    xeroTenantId: string,
    contactID: string,
    attachmentID: string,
    contentType: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<NodeJS.ReadableStream | Blob>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      contactID: [contactID, string()],
      attachmentID: [attachmentID, string()],
      contentType: [contentType, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('contentType', mapped.contentType);
    req.appendTemplatePath`/Contacts/${mapped.contactID}/Attachments/${mapped.attachmentID}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsStream(requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param contactID      Unique identifier for a Contact
   * @param fileName       Name for the file you are attaching
   * @param contentType    The mime type of the attachment file you are retrieving i.e image/jpg,
   *                                 application/pdf
   * @return Response from the API call
   */
  async getContactAttachmentByFileName(
    xeroTenantId: string,
    contactID: string,
    fileName: string,
    contentType: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<NodeJS.ReadableStream | Blob>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      contactID: [contactID, string()],
      fileName: [fileName, string()],
      contentType: [contentType, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('contentType', mapped.contentType);
    req.appendTemplatePath`/Contacts/${mapped.contactID}/Attachments/${mapped.fileName}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsStream(requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param contactID      Unique identifier for a Contact
   * @param fileName       Name for the file you are attaching
   * @param body           Byte array of file in body of request
   * @return Response from the API call
   */
  async updateContactAttachmentByFileName(
    xeroTenantId: string,
    contactID: string,
    fileName: string,
    body: FileWrapper,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Attachments>> {
    const req = this.createRequest('POST');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      contactID: [contactID, string()],
      fileName: [fileName, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/octet-stream');
    req.formData({ body: body });
    req.appendTemplatePath`/Contacts/${mapped.contactID}/Attachments/${mapped.fileName}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(attachmentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param contactID      Unique identifier for a Contact
   * @param fileName       Name for the file you are attaching
   * @param body           Byte array of file in body of request
   * @return Response from the API call
   */
  async createContactAttachmentByFileName(
    xeroTenantId: string,
    contactID: string,
    fileName: string,
    body: FileWrapper,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Attachments>> {
    const req = this.createRequest('PUT');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      contactID: [contactID, string()],
      fileName: [fileName, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/octet-stream');
    req.formData({ body: body });
    req.appendTemplatePath`/Contacts/${mapped.contactID}/Attachments/${mapped.fileName}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(attachmentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param contactID      Unique identifier for a Contact
   * @return Response from the API call
   */
  async getContactCISSettings(
    xeroTenantId: string,
    contactID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<CISSettings>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      contactID: [contactID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/Contacts/${mapped.contactID}/CISSettings`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(cISSettingsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param contactID      Unique identifier for a Contact
   * @return Response from the API call
   */
  async getContactHistory(
    xeroTenantId: string,
    contactID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoryRecords>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      contactID: [contactID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/Contacts/${mapped.contactID}/History`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(historyRecordsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param contactID      Unique identifier for a Contact
   * @param body           HistoryRecords containing an array of HistoryRecord objects in
   *                                                body of request
   * @return Response from the API call
   */
  async createContactHistory(
    xeroTenantId: string,
    contactID: string,
    body: HistoryRecords,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoryRecords>> {
    const req = this.createRequest('PUT');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      contactID: [contactID, string()],
      body: [body, historyRecordsSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/Contacts/${mapped.contactID}/History`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(historyRecordsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param contactNumber  This field is read only on the Xero contact screen, used to identify contacts in
   *                                 external systems (max length = 50).
   * @return Response from the API call
   */
  async getContactByContactNumber(
    xeroTenantId: string,
    contactNumber: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Contacts>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      contactNumber: [contactNumber, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/Contacts/${mapped.contactNumber}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(contactsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId      Xero identifier for Tenant
   * @param ifModifiedSince   Only records created or modified since this timestamp will be returned
   * @param where             Filter by an any element
   * @param order             Order by an any element
   * @param page              e.g. page=1 – Up to 100 credit notes will be returned in a single API call
   *                                    with line items shown for each credit note
   * @param unitdp            e.g. unitdp=4 – (Unit Decimal Places) You can opt in to use four decimal
   *                                    places for unit amounts
   * @return Response from the API call
   */
  async getCreditNotes(
    xeroTenantId: string,
    ifModifiedSince?: string,
    where?: string,
    order?: string,
    page?: number,
    unitdp?: number,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<CreditNotes>> {
    const req = this.createRequest('GET', '/CreditNotes');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      ifModifiedSince: [ifModifiedSince, optional(string())],
      where: [where, optional(string())],
      order: [order, optional(string())],
      page: [page, optional(number())],
      unitdp: [unitdp, optional(number())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('If-Modified-Since', mapped.ifModifiedSince);
    req.query('where', mapped.where);
    req.query('order', mapped.order);
    req.query('page', mapped.page);
    req.query('unitdp', mapped.unitdp);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(creditNotesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param body            an array of Credit Notes with a single CreditNote object.
   * @param summarizeErrors If false return 200 OK and mix of successfully created objects and
   *                                              any with validation errors
   * @param unitdp          e.g. unitdp=4 – (Unit Decimal Places) You can opt in to use four
   *                                              decimal places for unit amounts
   * @return Response from the API call
   */
  async updateOrCreateCreditNotes(
    xeroTenantId: string,
    body: CreditNotes,
    summarizeErrors?: boolean,
    unitdp?: number,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<CreditNotes>> {
    const req = this.createRequest('POST', '/CreditNotes');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      body: [body, creditNotesSchema],
      summarizeErrors: [summarizeErrors, optional(boolean())],
      unitdp: [unitdp, optional(number())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.query('summarizeErrors', mapped.summarizeErrors);
    req.query('unitdp', mapped.unitdp);
    req.json(mapped.body);
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(creditNotesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param body            Credit Notes with array of CreditNote object in body of request
   * @param summarizeErrors If false return 200 OK and mix of successfully created objects and
   *                                              any with validation errors
   * @param unitdp          e.g. unitdp=4 – (Unit Decimal Places) You can opt in to use four
   *                                              decimal places for unit amounts
   * @return Response from the API call
   */
  async createCreditNotes(
    xeroTenantId: string,
    body: CreditNotes,
    summarizeErrors?: boolean,
    unitdp?: number,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<CreditNotes>> {
    const req = this.createRequest('PUT', '/CreditNotes');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      body: [body, creditNotesSchema],
      summarizeErrors: [summarizeErrors, optional(boolean())],
      unitdp: [unitdp, optional(number())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.query('summarizeErrors', mapped.summarizeErrors);
    req.query('unitdp', mapped.unitdp);
    req.json(mapped.body);
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(creditNotesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param creditNoteID   Unique identifier for a Credit Note
   * @param unitdp         e.g. unitdp=4 – (Unit Decimal Places) You can opt in to use four decimal places
   *                                 for unit amounts
   * @return Response from the API call
   */
  async getCreditNote(
    xeroTenantId: string,
    creditNoteID: string,
    unitdp?: number,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<CreditNotes>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      creditNoteID: [creditNoteID, string()],
      unitdp: [unitdp, optional(number())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.query('unitdp', mapped.unitdp);
    req.appendTemplatePath`/CreditNotes/${mapped.creditNoteID}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(creditNotesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param creditNoteID   Unique identifier for a Credit Note
   * @param body           an array of Credit Notes containing credit note details to update
   * @param unitdp         e.g. unitdp=4 – (Unit Decimal Places) You can opt in to use four
   *                                             decimal places for unit amounts
   * @return Response from the API call
   */
  async updateCreditNote(
    xeroTenantId: string,
    creditNoteID: string,
    body: CreditNotes,
    unitdp?: number,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<CreditNotes>> {
    const req = this.createRequest('POST');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      creditNoteID: [creditNoteID, string()],
      body: [body, creditNotesSchema],
      unitdp: [unitdp, optional(number())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.query('unitdp', mapped.unitdp);
    req.json(mapped.body);
    req.appendTemplatePath`/CreditNotes/${mapped.creditNoteID}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(creditNotesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param creditNoteID    Unique identifier for a Credit Note
   * @param body            Allocations with array of Allocation object in body of request.
   * @param summarizeErrors If false return 200 OK and mix of successfully created objects and
   *                                              any with validation errors
   * @return Response from the API call
   */
  async createCreditNoteAllocation(
    xeroTenantId: string,
    creditNoteID: string,
    body: Allocations,
    summarizeErrors?: boolean,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Allocations>> {
    const req = this.createRequest('PUT');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      creditNoteID: [creditNoteID, string()],
      body: [body, allocationsSchema],
      summarizeErrors: [summarizeErrors, optional(boolean())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.query('summarizeErrors', mapped.summarizeErrors);
    req.json(mapped.body);
    req.appendTemplatePath`/CreditNotes/${mapped.creditNoteID}/Allocations`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(allocationsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param creditNoteID   Unique identifier for a Credit Note
   * @return Response from the API call
   */
  async getCreditNoteAttachments(
    xeroTenantId: string,
    creditNoteID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Attachments>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      creditNoteID: [creditNoteID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/CreditNotes/${mapped.creditNoteID}/Attachments`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(attachmentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param creditNoteID   Unique identifier for a Credit Note
   * @param attachmentID   Unique identifier for a Attachment
   * @param contentType    The mime type of the attachment file you are retrieving i.e image/jpg,
   *                                 application/pdf
   * @return Response from the API call
   */
  async getCreditNoteAttachmentById(
    xeroTenantId: string,
    creditNoteID: string,
    attachmentID: string,
    contentType: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<NodeJS.ReadableStream | Blob>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      creditNoteID: [creditNoteID, string()],
      attachmentID: [attachmentID, string()],
      contentType: [contentType, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('contentType', mapped.contentType);
    req.appendTemplatePath`/CreditNotes/${mapped.creditNoteID}/Attachments/${mapped.attachmentID}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsStream(requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param creditNoteID   Unique identifier for a Credit Note
   * @param fileName       Name of the file you are attaching to Credit Note
   * @param contentType    The mime type of the attachment file you are retrieving i.e image/jpg,
   *                                 application/pdf
   * @return Response from the API call
   */
  async getCreditNoteAttachmentByFileName(
    xeroTenantId: string,
    creditNoteID: string,
    fileName: string,
    contentType: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<NodeJS.ReadableStream | Blob>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      creditNoteID: [creditNoteID, string()],
      fileName: [fileName, string()],
      contentType: [contentType, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('contentType', mapped.contentType);
    req.appendTemplatePath`/CreditNotes/${mapped.creditNoteID}/Attachments/${mapped.fileName}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsStream(requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param creditNoteID   Unique identifier for a Credit Note
   * @param fileName       Name of the file you are attaching to Credit Note
   * @param body           Byte array of file in body of request
   * @return Response from the API call
   */
  async updateCreditNoteAttachmentByFileName(
    xeroTenantId: string,
    creditNoteID: string,
    fileName: string,
    body: FileWrapper,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Attachments>> {
    const req = this.createRequest('POST');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      creditNoteID: [creditNoteID, string()],
      fileName: [fileName, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/octet-stream');
    req.formData({ body: body });
    req.appendTemplatePath`/CreditNotes/${mapped.creditNoteID}/Attachments/${mapped.fileName}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(attachmentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param creditNoteID   Unique identifier for a Credit Note
   * @param fileName       Name of the file you are attaching to Credit Note
   * @param body           Byte array of file in body of request
   * @param includeOnline  Allows an attachment to be seen by the end customer within their online
   *                                      invoice
   * @return Response from the API call
   */
  async createCreditNoteAttachmentByFileName(
    xeroTenantId: string,
    creditNoteID: string,
    fileName: string,
    body: FileWrapper,
    includeOnline?: boolean,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Attachments>> {
    const req = this.createRequest('PUT');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      creditNoteID: [creditNoteID, string()],
      fileName: [fileName, string()],
      includeOnline: [includeOnline, optional(boolean())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/octet-stream');
    req.query('IncludeOnline', mapped.includeOnline);
    req.formData({ body: body });
    req.appendTemplatePath`/CreditNotes/${mapped.creditNoteID}/Attachments/${mapped.fileName}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(attachmentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param creditNoteID   Unique identifier for a Credit Note
   * @return Response from the API call
   */
  async getCreditNoteHistory(
    xeroTenantId: string,
    creditNoteID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoryRecords>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      creditNoteID: [creditNoteID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/CreditNotes/${mapped.creditNoteID}/History`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(historyRecordsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param creditNoteID   Unique identifier for a Credit Note
   * @param body           HistoryRecords containing an array of HistoryRecord objects in
   *                                                body of request
   * @return Response from the API call
   */
  async createCreditNoteHistory(
    xeroTenantId: string,
    creditNoteID: string,
    body: HistoryRecords,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoryRecords>> {
    const req = this.createRequest('PUT');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      creditNoteID: [creditNoteID, string()],
      body: [body, historyRecordsSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/CreditNotes/${mapped.creditNoteID}/History`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(historyRecordsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param creditNoteID   Unique identifier for a Credit Note
   * @return Response from the API call
   */
  async getCreditNoteAsPdf(
    xeroTenantId: string,
    creditNoteID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<unknown>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      creditNoteID: [creditNoteID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/CreditNotes/${mapped.creditNoteID}/pdf`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(unknown(), requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param where          Filter by an any element
   * @param order          Order by an any element
   * @return Response from the API call
   */
  async getCurrencies(
    xeroTenantId: string,
    where?: string,
    order?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Currencies>> {
    const req = this.createRequest('GET', '/Currencies');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      where: [where, optional(string())],
      order: [order, optional(string())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.query('where', mapped.where);
    req.query('order', mapped.order);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(currenciesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param body           Currency object in the body of request
   * @return Response from the API call
   */
  async createCurrency(
    xeroTenantId: string,
    body: Currency,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Currencies>> {
    const req = this.createRequest('PUT', '/Currencies');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      body: [body, currencySchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(currenciesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId      Xero identifier for Tenant
   * @param ifModifiedSince   Only records created or modified since this timestamp will be returned
   * @param where             Filter by an any element
   * @param order             Order by an any element
   * @return Response from the API call
   */
  async getEmployees(
    xeroTenantId: string,
    ifModifiedSince?: string,
    where?: string,
    order?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Employees>> {
    const req = this.createRequest('GET', '/Employees');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      ifModifiedSince: [ifModifiedSince, optional(string())],
      where: [where, optional(string())],
      order: [order, optional(string())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('If-Modified-Since', mapped.ifModifiedSince);
    req.query('where', mapped.where);
    req.query('order', mapped.order);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(employeesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param body            Employees with array of Employee object in body of request
   * @param summarizeErrors If false return 200 OK and mix of successfully created objects and any
   *                                            with validation errors
   * @return Response from the API call
   */
  async updateOrCreateEmployees(
    xeroTenantId: string,
    body: Employees,
    summarizeErrors?: boolean,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Employees>> {
    const req = this.createRequest('POST', '/Employees');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      body: [body, employeesSchema],
      summarizeErrors: [summarizeErrors, optional(boolean())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.query('summarizeErrors', mapped.summarizeErrors);
    req.json(mapped.body);
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(employeesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param body            Employees with array of Employee object in body of request
   * @param summarizeErrors If false return 200 OK and mix of successfully created objects and any
   *                                            with validation errors
   * @return Response from the API call
   */
  async createEmployees(
    xeroTenantId: string,
    body: Employees,
    summarizeErrors?: boolean,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Employees>> {
    const req = this.createRequest('PUT', '/Employees');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      body: [body, employeesSchema],
      summarizeErrors: [summarizeErrors, optional(boolean())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.query('summarizeErrors', mapped.summarizeErrors);
    req.json(mapped.body);
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(employeesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param employeeID     Unique identifier for a Employee
   * @return Response from the API call
   */
  async getEmployee(
    xeroTenantId: string,
    employeeID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Employees>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      employeeID: [employeeID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/Employees/${mapped.employeeID}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(employeesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId      Xero identifier for Tenant
   * @param ifModifiedSince   Only records created or modified since this timestamp will be returned
   * @param where             Filter by an any element
   * @param order             Order by an any element
   * @return Response from the API call
   */
  async getExpenseClaims(
    xeroTenantId: string,
    ifModifiedSince?: string,
    where?: string,
    order?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ExpenseClaims>> {
    const req = this.createRequest('GET', '/ExpenseClaims');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      ifModifiedSince: [ifModifiedSince, optional(string())],
      where: [where, optional(string())],
      order: [order, optional(string())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('If-Modified-Since', mapped.ifModifiedSince);
    req.query('where', mapped.where);
    req.query('order', mapped.order);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(expenseClaimsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param body           ExpenseClaims with array of ExpenseClaim object in body of request
   * @return Response from the API call
   */
  async createExpenseClaims(
    xeroTenantId: string,
    body: ExpenseClaims,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ExpenseClaims>> {
    const req = this.createRequest('PUT', '/ExpenseClaims');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      body: [body, expenseClaimsSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(expenseClaimsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param expenseClaimID Unique identifier for a ExpenseClaim
   * @return Response from the API call
   */
  async getExpenseClaim(
    xeroTenantId: string,
    expenseClaimID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ExpenseClaims>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      expenseClaimID: [expenseClaimID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/ExpenseClaims/${mapped.expenseClaimID}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(expenseClaimsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param expenseClaimID Unique identifier for a ExpenseClaim
   * @param body
   * @return Response from the API call
   */
  async updateExpenseClaim(
    xeroTenantId: string,
    expenseClaimID: string,
    body: ExpenseClaims,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ExpenseClaims>> {
    const req = this.createRequest('POST');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      expenseClaimID: [expenseClaimID, string()],
      body: [body, expenseClaimsSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/ExpenseClaims/${mapped.expenseClaimID}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(expenseClaimsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param expenseClaimID Unique identifier for a ExpenseClaim
   * @return Response from the API call
   */
  async getExpenseClaimHistory(
    xeroTenantId: string,
    expenseClaimID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoryRecords>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      expenseClaimID: [expenseClaimID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/ExpenseClaims/${mapped.expenseClaimID}/History`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(historyRecordsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param expenseClaimID Unique identifier for a ExpenseClaim
   * @param body           HistoryRecords containing an array of HistoryRecord objects in
   *                                                body of request
   * @return Response from the API call
   */
  async createExpenseClaimHistory(
    xeroTenantId: string,
    expenseClaimID: string,
    body: HistoryRecords,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoryRecords>> {
    const req = this.createRequest('PUT');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      expenseClaimID: [expenseClaimID, string()],
      body: [body, historyRecordsSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/ExpenseClaims/${mapped.expenseClaimID}/History`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(historyRecordsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @return Response from the API call
   */
  async getInvoiceReminders(
    xeroTenantId: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<InvoiceReminders>> {
    const req = this.createRequest('GET', '/InvoiceReminders/Settings');
    const mapped = req.prepareArgs({ xeroTenantId: [xeroTenantId, string()] });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(invoiceRemindersSchema, requestOptions);
  }

  /**
   * @param xeroTenantId      Xero identifier for Tenant
   * @param ifModifiedSince   Only records created or modified since this timestamp will be returned
   * @param where             Filter by an any element
   * @param order             Order by an any element
   * @param iDs               Filter by a comma-separated list of InvoicesIDs.
   * @param invoiceNumbers    Filter by a comma-separated list of InvoiceNumbers.
   * @param contactIDs        Filter by a comma-separated list of ContactIDs.
   * @param statuses          Filter by a comma-separated list Statuses. For faster response times we
   *                                      recommend using these explicit parameters instead of passing OR conditions
   *                                      into the Where filter.
   * @param page              e.g. page=1 – Up to 100 invoices will be returned in a single API call with
   *                                      line items shown for each invoice
   * @param includeArchived   e.g. includeArchived=true - Contacts with a status of ARCHIVED will be
   *                                      included in the response
   * @param createdByMyApp    When set to true you'll only retrieve Invoices created by your app
   * @param unitdp            e.g. unitdp=4 – (Unit Decimal Places) You can opt in to use four decimal
   *                                      places for unit amounts
   * @return Response from the API call
   */
  async getInvoices(
    xeroTenantId: string,
    ifModifiedSince?: string,
    where?: string,
    order?: string,
    iDs?: string[],
    invoiceNumbers?: string[],
    contactIDs?: string[],
    statuses?: string[],
    page?: number,
    includeArchived?: boolean,
    createdByMyApp?: boolean,
    unitdp?: number,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Invoices>> {
    const req = this.createRequest('GET', '/Invoices');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      ifModifiedSince: [ifModifiedSince, optional(string())],
      where: [where, optional(string())],
      order: [order, optional(string())],
      iDs: [iDs, optional(array(string()))],
      invoiceNumbers: [invoiceNumbers, optional(array(string()))],
      contactIDs: [contactIDs, optional(array(string()))],
      statuses: [statuses, optional(array(string()))],
      page: [page, optional(number())],
      includeArchived: [includeArchived, optional(boolean())],
      createdByMyApp: [createdByMyApp, optional(boolean())],
      unitdp: [unitdp, optional(number())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('If-Modified-Since', mapped.ifModifiedSince);
    req.query('where', mapped.where);
    req.query('order', mapped.order);
    req.query('IDs', mapped.iDs);
    req.query('InvoiceNumbers', mapped.invoiceNumbers);
    req.query('ContactIDs', mapped.contactIDs);
    req.query('Statuses', mapped.statuses);
    req.query('page', mapped.page);
    req.query('includeArchived', mapped.includeArchived);
    req.query('createdByMyApp', mapped.createdByMyApp);
    req.query('unitdp', mapped.unitdp);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(invoicesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param body
   * @param summarizeErrors If false return 200 OK and mix of successfully created objects and any
   *                                           with validation errors
   * @param unitdp          e.g. unitdp=4 – (Unit Decimal Places) You can opt in to use four
   *                                           decimal places for unit amounts
   * @return Response from the API call
   */
  async updateOrCreateInvoices(
    xeroTenantId: string,
    body: Invoices,
    summarizeErrors?: boolean,
    unitdp?: number,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Invoices>> {
    const req = this.createRequest('POST', '/Invoices');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      body: [body, invoicesSchema],
      summarizeErrors: [summarizeErrors, optional(boolean())],
      unitdp: [unitdp, optional(number())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.query('summarizeErrors', mapped.summarizeErrors);
    req.query('unitdp', mapped.unitdp);
    req.json(mapped.body);
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(invoicesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param body            Invoices with an array of invoice objects in body of request
   * @param summarizeErrors If false return 200 OK and mix of successfully created objects and any
   *                                           with validation errors
   * @param unitdp          e.g. unitdp=4 – (Unit Decimal Places) You can opt in to use four
   *                                           decimal places for unit amounts
   * @return Response from the API call
   */
  async createInvoices(
    xeroTenantId: string,
    body: Invoices,
    summarizeErrors?: boolean,
    unitdp?: number,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Invoices>> {
    const req = this.createRequest('PUT', '/Invoices');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      body: [body, invoicesSchema],
      summarizeErrors: [summarizeErrors, optional(boolean())],
      unitdp: [unitdp, optional(number())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.query('summarizeErrors', mapped.summarizeErrors);
    req.query('unitdp', mapped.unitdp);
    req.json(mapped.body);
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(invoicesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param invoiceID      Unique identifier for an Invoice
   * @param unitdp         e.g. unitdp=4 – (Unit Decimal Places) You can opt in to use four decimal places
   *                                 for unit amounts
   * @return Response from the API call
   */
  async getInvoice(
    xeroTenantId: string,
    invoiceID: string,
    unitdp?: number,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Invoices>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      invoiceID: [invoiceID, string()],
      unitdp: [unitdp, optional(number())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.query('unitdp', mapped.unitdp);
    req.appendTemplatePath`/Invoices/${mapped.invoiceID}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(invoicesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param invoiceID      Unique identifier for an Invoice
   * @param body
   * @param unitdp         e.g. unitdp=4 – (Unit Decimal Places) You can opt in to use four decimal
   *                                          places for unit amounts
   * @return Response from the API call
   */
  async updateInvoice(
    xeroTenantId: string,
    invoiceID: string,
    body: Invoices,
    unitdp?: number,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Invoices>> {
    const req = this.createRequest('POST');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      invoiceID: [invoiceID, string()],
      body: [body, invoicesSchema],
      unitdp: [unitdp, optional(number())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.query('unitdp', mapped.unitdp);
    req.json(mapped.body);
    req.appendTemplatePath`/Invoices/${mapped.invoiceID}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(invoicesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param invoiceID      Unique identifier for an Invoice
   * @return Response from the API call
   */
  async getInvoiceAttachments(
    xeroTenantId: string,
    invoiceID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Attachments>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      invoiceID: [invoiceID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/Invoices/${mapped.invoiceID}/Attachments`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(attachmentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param invoiceID      Unique identifier for an Invoice
   * @param attachmentID   Unique identifier for an Attachment
   * @param contentType    The mime type of the attachment file you are retrieving i.e image/jpg,
   *                                 application/pdf
   * @return Response from the API call
   */
  async getInvoiceAttachmentById(
    xeroTenantId: string,
    invoiceID: string,
    attachmentID: string,
    contentType: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<NodeJS.ReadableStream | Blob>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      invoiceID: [invoiceID, string()],
      attachmentID: [attachmentID, string()],
      contentType: [contentType, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('contentType', mapped.contentType);
    req.appendTemplatePath`/Invoices/${mapped.invoiceID}/Attachments/${mapped.attachmentID}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsStream(requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param invoiceID      Unique identifier for an Invoice
   * @param fileName       Name of the file you are attaching
   * @param contentType    The mime type of the attachment file you are retrieving i.e image/jpg,
   *                                 application/pdf
   * @return Response from the API call
   */
  async getInvoiceAttachmentByFileName(
    xeroTenantId: string,
    invoiceID: string,
    fileName: string,
    contentType: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<NodeJS.ReadableStream | Blob>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      invoiceID: [invoiceID, string()],
      fileName: [fileName, string()],
      contentType: [contentType, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('contentType', mapped.contentType);
    req.appendTemplatePath`/Invoices/${mapped.invoiceID}/Attachments/${mapped.fileName}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsStream(requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param invoiceID      Unique identifier for an Invoice
   * @param fileName       Name of the file you are attaching
   * @param body           Byte array of file in body of request
   * @return Response from the API call
   */
  async updateInvoiceAttachmentByFileName(
    xeroTenantId: string,
    invoiceID: string,
    fileName: string,
    body: FileWrapper,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Attachments>> {
    const req = this.createRequest('POST');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      invoiceID: [invoiceID, string()],
      fileName: [fileName, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/octet-stream');
    req.formData({ body: body });
    req.appendTemplatePath`/Invoices/${mapped.invoiceID}/Attachments/${mapped.fileName}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(attachmentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param invoiceID      Unique identifier for an Invoice
   * @param fileName       Name of the file you are attaching
   * @param body           Byte array of file in body of request
   * @param includeOnline  Allows an attachment to be seen by the end customer within their online
   *                                      invoice
   * @return Response from the API call
   */
  async createInvoiceAttachmentByFileName(
    xeroTenantId: string,
    invoiceID: string,
    fileName: string,
    body: FileWrapper,
    includeOnline?: boolean,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Attachments>> {
    const req = this.createRequest('PUT');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      invoiceID: [invoiceID, string()],
      fileName: [fileName, string()],
      includeOnline: [includeOnline, optional(boolean())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/octet-stream');
    req.query('IncludeOnline', mapped.includeOnline);
    req.formData({ body: body });
    req.appendTemplatePath`/Invoices/${mapped.invoiceID}/Attachments/${mapped.fileName}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(attachmentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param invoiceID      Unique identifier for an Invoice
   * @param body
   * @return Response from the API call
   */
  async emailInvoice(
    xeroTenantId: string,
    invoiceID: string,
    body: RequestEmpty,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<void>> {
    const req = this.createRequest('POST');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      invoiceID: [invoiceID, string()],
      body: [body, requestEmptySchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/Invoices/${mapped.invoiceID}/Email`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.call(requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param invoiceID      Unique identifier for an Invoice
   * @return Response from the API call
   */
  async getInvoiceHistory(
    xeroTenantId: string,
    invoiceID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoryRecords>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      invoiceID: [invoiceID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/Invoices/${mapped.invoiceID}/History`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(historyRecordsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param invoiceID      Unique identifier for an Invoice
   * @param body           HistoryRecords containing an array of HistoryRecord objects in
   *                                                body of request
   * @return Response from the API call
   */
  async createInvoiceHistory(
    xeroTenantId: string,
    invoiceID: string,
    body: HistoryRecords,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoryRecords>> {
    const req = this.createRequest('PUT');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      invoiceID: [invoiceID, string()],
      body: [body, historyRecordsSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/Invoices/${mapped.invoiceID}/History`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(historyRecordsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param invoiceID      Unique identifier for an Invoice
   * @return Response from the API call
   */
  async getOnlineInvoice(
    xeroTenantId: string,
    invoiceID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<OnlineInvoices>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      invoiceID: [invoiceID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/Invoices/${mapped.invoiceID}/OnlineInvoice`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(onlineInvoicesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param invoiceID      Unique identifier for an Invoice
   * @return Response from the API call
   */
  async getInvoiceAsPdf(
    xeroTenantId: string,
    invoiceID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<unknown>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      invoiceID: [invoiceID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/Invoices/${mapped.invoiceID}/pdf`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(unknown(), requestOptions);
  }

  /**
   * @param xeroTenantId      Xero identifier for Tenant
   * @param ifModifiedSince   Only records created or modified since this timestamp will be returned
   * @param where             Filter by an any element
   * @param order             Order by an any element
   * @param unitdp            e.g. unitdp=4 – (Unit Decimal Places) You can opt in to use four decimal
   *                                    places for unit amounts
   * @return Response from the API call
   */
  async getItems(
    xeroTenantId: string,
    ifModifiedSince?: string,
    where?: string,
    order?: string,
    unitdp?: number,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Items>> {
    const req = this.createRequest('GET', '/Items');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      ifModifiedSince: [ifModifiedSince, optional(string())],
      where: [where, optional(string())],
      order: [order, optional(string())],
      unitdp: [unitdp, optional(number())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('If-Modified-Since', mapped.ifModifiedSince);
    req.query('where', mapped.where);
    req.query('order', mapped.order);
    req.query('unitdp', mapped.unitdp);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(itemsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param body
   * @param summarizeErrors If false return 200 OK and mix of successfully created objects and any
   *                                        with validation errors
   * @param unitdp          e.g. unitdp=4 – (Unit Decimal Places) You can opt in to use four decimal
   *                                        places for unit amounts
   * @return Response from the API call
   */
  async updateOrCreateItems(
    xeroTenantId: string,
    body: Items,
    summarizeErrors?: boolean,
    unitdp?: number,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Items>> {
    const req = this.createRequest('POST', '/Items');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      body: [body, itemsSchema],
      summarizeErrors: [summarizeErrors, optional(boolean())],
      unitdp: [unitdp, optional(number())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.query('summarizeErrors', mapped.summarizeErrors);
    req.query('unitdp', mapped.unitdp);
    req.json(mapped.body);
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(itemsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param body            Items with an array of Item objects in body of request
   * @param summarizeErrors If false return 200 OK and mix of successfully created objects and any
   *                                        with validation errors
   * @param unitdp          e.g. unitdp=4 – (Unit Decimal Places) You can opt in to use four decimal
   *                                        places for unit amounts
   * @return Response from the API call
   */
  async createItems(
    xeroTenantId: string,
    body: Items,
    summarizeErrors?: boolean,
    unitdp?: number,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Items>> {
    const req = this.createRequest('PUT', '/Items');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      body: [body, itemsSchema],
      summarizeErrors: [summarizeErrors, optional(boolean())],
      unitdp: [unitdp, optional(number())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.query('summarizeErrors', mapped.summarizeErrors);
    req.query('unitdp', mapped.unitdp);
    req.json(mapped.body);
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(itemsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param itemID         Unique identifier for an Item
   * @return Response from the API call
   */
  async deleteItem(
    xeroTenantId: string,
    itemID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<void>> {
    const req = this.createRequest('DELETE');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      itemID: [itemID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/Items/${mapped.itemID}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.call(requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param itemID         Unique identifier for an Item
   * @param unitdp         e.g. unitdp=4 – (Unit Decimal Places) You can opt in to use four decimal places
   *                                 for unit amounts
   * @return Response from the API call
   */
  async getItem(
    xeroTenantId: string,
    itemID: string,
    unitdp?: number,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Items>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      itemID: [itemID, string()],
      unitdp: [unitdp, optional(number())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.query('unitdp', mapped.unitdp);
    req.appendTemplatePath`/Items/${mapped.itemID}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(itemsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param itemID         Unique identifier for an Item
   * @param body
   * @param unitdp         e.g. unitdp=4 – (Unit Decimal Places) You can opt in to use four decimal
   *                                       places for unit amounts
   * @return Response from the API call
   */
  async updateItem(
    xeroTenantId: string,
    itemID: string,
    body: Items,
    unitdp?: number,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Items>> {
    const req = this.createRequest('POST');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      itemID: [itemID, string()],
      body: [body, itemsSchema],
      unitdp: [unitdp, optional(number())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.query('unitdp', mapped.unitdp);
    req.json(mapped.body);
    req.appendTemplatePath`/Items/${mapped.itemID}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(itemsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param itemID         Unique identifier for an Item
   * @return Response from the API call
   */
  async getItemHistory(
    xeroTenantId: string,
    itemID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoryRecords>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      itemID: [itemID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/Items/${mapped.itemID}/History`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(historyRecordsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param itemID         Unique identifier for an Item
   * @param body           HistoryRecords containing an array of HistoryRecord objects in
   *                                                body of request
   * @return Response from the API call
   */
  async createItemHistory(
    xeroTenantId: string,
    itemID: string,
    body: HistoryRecords,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoryRecords>> {
    const req = this.createRequest('PUT');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      itemID: [itemID, string()],
      body: [body, historyRecordsSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/Items/${mapped.itemID}/History`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(historyRecordsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId      Xero identifier for Tenant
   * @param ifModifiedSince   Only records created or modified since this timestamp will be returned
   * @param offset            Offset by a specified journal number. e.g. journals with a JournalNumber
   *                                     greater than the offset will be returned
   * @param paymentsOnly      Filter to retrieve journals on a cash basis. Journals are returned on an
   *                                     accrual basis by default.
   * @return Response from the API call
   */
  async getJournals(
    xeroTenantId: string,
    ifModifiedSince?: string,
    offset?: number,
    paymentsOnly?: boolean,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Journals>> {
    const req = this.createRequest('GET', '/Journals');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      ifModifiedSince: [ifModifiedSince, optional(string())],
      offset: [offset, optional(number())],
      paymentsOnly: [paymentsOnly, optional(boolean())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('If-Modified-Since', mapped.ifModifiedSince);
    req.query('offset', mapped.offset);
    req.query('paymentsOnly', mapped.paymentsOnly);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(journalsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param journalID      Unique identifier for a Journal
   * @return Response from the API call
   */
  async getJournal(
    xeroTenantId: string,
    journalID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Journals>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      journalID: [journalID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/Journals/${mapped.journalID}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(journalsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId        Xero identifier for Tenant
   * @param page                Up to 100 linked transactions will be returned in a single API call. Use the
   *                                      page parameter to specify the page to be returned e.g. page=1.
   * @param linkedTransactionID The Xero identifier for an Linked Transaction
   * @param sourceTransactionID Filter by the SourceTransactionID. Get the linked transactions created from
   *                                      a particular ACCPAY invoice
   * @param contactID           Filter by the ContactID. Get all the linked transactions that have been
   *                                      assigned to a particular customer.
   * @param status              Filter by the combination of ContactID and Status. Get  the linked
   *                                      transactions associated to a  customer and with a status
   * @param targetTransactionID Filter by the TargetTransactionID. Get all the linked transactions allocated
   *                                      to a particular ACCREC invoice
   * @return Response from the API call
   */
  async getLinkedTransactions(
    xeroTenantId: string,
    page?: number,
    linkedTransactionID?: string,
    sourceTransactionID?: string,
    contactID?: string,
    status?: string,
    targetTransactionID?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<LinkedTransactions>> {
    const req = this.createRequest('GET', '/LinkedTransactions');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      page: [page, optional(number())],
      linkedTransactionID: [linkedTransactionID, optional(string())],
      sourceTransactionID: [sourceTransactionID, optional(string())],
      contactID: [contactID, optional(string())],
      status: [status, optional(string())],
      targetTransactionID: [targetTransactionID, optional(string())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.query('page', mapped.page);
    req.query('LinkedTransactionID', mapped.linkedTransactionID);
    req.query('SourceTransactionID', mapped.sourceTransactionID);
    req.query('ContactID', mapped.contactID);
    req.query('Status', mapped.status);
    req.query('TargetTransactionID', mapped.targetTransactionID);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(linkedTransactionsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param body           LinkedTransaction object in body of request
   * @return Response from the API call
   */
  async createLinkedTransaction(
    xeroTenantId: string,
    body: LinkedTransaction,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<LinkedTransactions>> {
    const req = this.createRequest('PUT', '/LinkedTransactions');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      body: [body, linkedTransactionSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(linkedTransactionsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId        Xero identifier for Tenant
   * @param linkedTransactionID Unique identifier for a LinkedTransaction
   * @return Response from the API call
   */
  async deleteLinkedTransaction(
    xeroTenantId: string,
    linkedTransactionID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<void>> {
    const req = this.createRequest('DELETE');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      linkedTransactionID: [linkedTransactionID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/LinkedTransactions/${mapped.linkedTransactionID}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.call(requestOptions);
  }

  /**
   * @param xeroTenantId        Xero identifier for Tenant
   * @param linkedTransactionID Unique identifier for a LinkedTransaction
   * @return Response from the API call
   */
  async getLinkedTransaction(
    xeroTenantId: string,
    linkedTransactionID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<LinkedTransactions>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      linkedTransactionID: [linkedTransactionID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/LinkedTransactions/${mapped.linkedTransactionID}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(linkedTransactionsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId        Xero identifier for Tenant
   * @param linkedTransactionID Unique identifier for a LinkedTransaction
   * @param body
   * @return Response from the API call
   */
  async updateLinkedTransaction(
    xeroTenantId: string,
    linkedTransactionID: string,
    body: LinkedTransactions,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<LinkedTransactions>> {
    const req = this.createRequest('POST');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      linkedTransactionID: [linkedTransactionID, string()],
      body: [body, linkedTransactionsSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/LinkedTransactions/${mapped.linkedTransactionID}`;
    req.throwOn(
      400,
      CustomError,
      'Success - return response of type LinkedTransactions array with updated LinkedTransaction'
    );
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(linkedTransactionsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId      Xero identifier for Tenant
   * @param ifModifiedSince   Only records created or modified since this timestamp will be returned
   * @param where             Filter by an any element
   * @param order             Order by an any element
   * @param page              e.g. page=1 – Up to 100 manual journals will be returned in a single API call
   *                                    with line items shown for each overpayment
   * @return Response from the API call
   */
  async getManualJournals(
    xeroTenantId: string,
    ifModifiedSince?: string,
    where?: string,
    order?: string,
    page?: number,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ManualJournals>> {
    const req = this.createRequest('GET', '/ManualJournals');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      ifModifiedSince: [ifModifiedSince, optional(string())],
      where: [where, optional(string())],
      order: [order, optional(string())],
      page: [page, optional(number())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('If-Modified-Since', mapped.ifModifiedSince);
    req.query('where', mapped.where);
    req.query('order', mapped.order);
    req.query('page', mapped.page);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(manualJournalsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param body            ManualJournals array with ManualJournal object in body of
   *                                                 request
   * @param summarizeErrors If false return 200 OK and mix of successfully created objects
   *                                                 and any with validation errors
   * @return Response from the API call
   */
  async updateOrCreateManualJournals(
    xeroTenantId: string,
    body: ManualJournals,
    summarizeErrors?: boolean,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ManualJournals>> {
    const req = this.createRequest('POST', '/ManualJournals');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      body: [body, manualJournalsSchema],
      summarizeErrors: [summarizeErrors, optional(boolean())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.query('summarizeErrors', mapped.summarizeErrors);
    req.json(mapped.body);
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(manualJournalsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param body            ManualJournals array with ManualJournal object in body of
   *                                                 request
   * @param summarizeErrors If false return 200 OK and mix of successfully created objects
   *                                                 and any with validation errors
   * @return Response from the API call
   */
  async createManualJournals(
    xeroTenantId: string,
    body: ManualJournals,
    summarizeErrors?: boolean,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ManualJournals>> {
    const req = this.createRequest('PUT', '/ManualJournals');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      body: [body, manualJournalsSchema],
      summarizeErrors: [summarizeErrors, optional(boolean())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.query('summarizeErrors', mapped.summarizeErrors);
    req.json(mapped.body);
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(manualJournalsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param manualJournalID Unique identifier for a ManualJournal
   * @return Response from the API call
   */
  async getManualJournal(
    xeroTenantId: string,
    manualJournalID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ManualJournals>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      manualJournalID: [manualJournalID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/ManualJournals/${mapped.manualJournalID}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(manualJournalsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param manualJournalID Unique identifier for a ManualJournal
   * @param body
   * @return Response from the API call
   */
  async updateManualJournal(
    xeroTenantId: string,
    manualJournalID: string,
    body: ManualJournals,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ManualJournals>> {
    const req = this.createRequest('POST');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      manualJournalID: [manualJournalID, string()],
      body: [body, manualJournalsSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/ManualJournals/${mapped.manualJournalID}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(manualJournalsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param manualJournalID Unique identifier for a ManualJournal
   * @return Response from the API call
   */
  async getManualJournalAttachments(
    xeroTenantId: string,
    manualJournalID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Attachments>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      manualJournalID: [manualJournalID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/ManualJournals/${mapped.manualJournalID}/Attachments`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(attachmentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param manualJournalID Unique identifier for a ManualJournal
   * @param attachmentID    Unique identifier for a Attachment
   * @param contentType     The mime type of the attachment file you are retrieving i.e image/jpg,
   *                                  application/pdf
   * @return Response from the API call
   */
  async getManualJournalAttachmentById(
    xeroTenantId: string,
    manualJournalID: string,
    attachmentID: string,
    contentType: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<NodeJS.ReadableStream | Blob>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      manualJournalID: [manualJournalID, string()],
      attachmentID: [attachmentID, string()],
      contentType: [contentType, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('contentType', mapped.contentType);
    req.appendTemplatePath`/ManualJournals/${mapped.manualJournalID}/Attachments/${mapped.attachmentID}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsStream(requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param manualJournalID Unique identifier for a ManualJournal
   * @param fileName        The name of the file being attached to a ManualJournal
   * @param contentType     The mime type of the attachment file you are retrieving i.e image/jpg,
   *                                  application/pdf
   * @return Response from the API call
   */
  async getManualJournalAttachmentByFileName(
    xeroTenantId: string,
    manualJournalID: string,
    fileName: string,
    contentType: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<NodeJS.ReadableStream | Blob>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      manualJournalID: [manualJournalID, string()],
      fileName: [fileName, string()],
      contentType: [contentType, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('contentType', mapped.contentType);
    req.appendTemplatePath`/ManualJournals/${mapped.manualJournalID}/Attachments/${mapped.fileName}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsStream(requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param manualJournalID Unique identifier for a ManualJournal
   * @param fileName        The name of the file being attached to a ManualJournal
   * @param body            Byte array of file in body of request
   * @return Response from the API call
   */
  async updateManualJournalAttachmentByFileName(
    xeroTenantId: string,
    manualJournalID: string,
    fileName: string,
    body: FileWrapper,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Attachments>> {
    const req = this.createRequest('POST');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      manualJournalID: [manualJournalID, string()],
      fileName: [fileName, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/octet-stream');
    req.formData({ body: body });
    req.appendTemplatePath`/ManualJournals/${mapped.manualJournalID}/Attachments/${mapped.fileName}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(attachmentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param manualJournalID Unique identifier for a ManualJournal
   * @param fileName        The name of the file being attached to a ManualJournal
   * @param body            Byte array of file in body of request
   * @return Response from the API call
   */
  async createManualJournalAttachmentByFileName(
    xeroTenantId: string,
    manualJournalID: string,
    fileName: string,
    body: FileWrapper,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Attachments>> {
    const req = this.createRequest('PUT');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      manualJournalID: [manualJournalID, string()],
      fileName: [fileName, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/octet-stream');
    req.formData({ body: body });
    req.appendTemplatePath`/ManualJournals/${mapped.manualJournalID}/Attachments/${mapped.fileName}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(attachmentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param manualJournalID Xero generated unique identifier for a manual journal
   * @return Response from the API call
   */
  async getManualJournalsHistory(
    xeroTenantId: string,
    manualJournalID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoryRecords>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      manualJournalID: [manualJournalID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/ManualJournals/${mapped.manualJournalID}/History`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(historyRecordsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param manualJournalID Xero generated unique identifier for a manual journal
   * @param body            HistoryRecords containing an array of HistoryRecord objects in
   *                                                 body of request
   * @return Response from the API call
   */
  async createManualJournalHistoryRecord(
    xeroTenantId: string,
    manualJournalID: string,
    body: HistoryRecords,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoryRecords>> {
    const req = this.createRequest('PUT');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      manualJournalID: [manualJournalID, string()],
      body: [body, historyRecordsSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/ManualJournals/${mapped.manualJournalID}/History`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(historyRecordsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @return Response from the API call
   */
  async getOrganisations(
    xeroTenantId: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Organisations>> {
    const req = this.createRequest('GET', '/Organisation');
    const mapped = req.prepareArgs({ xeroTenantId: [xeroTenantId, string()] });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(organisationsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @return Response from the API call
   */
  async getOrganisationActions(
    xeroTenantId: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Actions>> {
    const req = this.createRequest('GET', '/Organisation/Actions');
    const mapped = req.prepareArgs({ xeroTenantId: [xeroTenantId, string()] });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(actionsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param organisationID The unique Xero identifier for an organisation
   * @return Response from the API call
   */
  async getOrganisationCISSettings(
    xeroTenantId: string,
    organisationID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<CISOrgSettings>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      organisationID: [organisationID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/Organisation/${mapped.organisationID}/CISSettings`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(cISOrgSettingsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId      Xero identifier for Tenant
   * @param ifModifiedSince   Only records created or modified since this timestamp will be returned
   * @param where             Filter by an any element
   * @param order             Order by an any element
   * @param page              e.g. page=1 – Up to 100 overpayments will be returned in a single API call
   *                                    with line items shown for each overpayment
   * @param unitdp            e.g. unitdp=4 – (Unit Decimal Places) You can opt in to use four decimal
   *                                    places for unit amounts
   * @return Response from the API call
   */
  async getOverpayments(
    xeroTenantId: string,
    ifModifiedSince?: string,
    where?: string,
    order?: string,
    page?: number,
    unitdp?: number,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Overpayments>> {
    const req = this.createRequest('GET', '/Overpayments');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      ifModifiedSince: [ifModifiedSince, optional(string())],
      where: [where, optional(string())],
      order: [order, optional(string())],
      page: [page, optional(number())],
      unitdp: [unitdp, optional(number())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('If-Modified-Since', mapped.ifModifiedSince);
    req.query('where', mapped.where);
    req.query('order', mapped.order);
    req.query('page', mapped.page);
    req.query('unitdp', mapped.unitdp);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(overpaymentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param overpaymentID  Unique identifier for a Overpayment
   * @return Response from the API call
   */
  async getOverpayment(
    xeroTenantId: string,
    overpaymentID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Overpayments>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      overpaymentID: [overpaymentID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/Overpayments/${mapped.overpaymentID}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(overpaymentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param overpaymentID   Unique identifier for a Overpayment
   * @param body            Allocations array with Allocation object in body of request
   * @param summarizeErrors If false return 200 OK and mix of successfully created objects and
   *                                              any with validation errors
   * @return Response from the API call
   */
  async createOverpaymentAllocations(
    xeroTenantId: string,
    overpaymentID: string,
    body: Allocations,
    summarizeErrors?: boolean,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Allocations>> {
    const req = this.createRequest('PUT');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      overpaymentID: [overpaymentID, string()],
      body: [body, allocationsSchema],
      summarizeErrors: [summarizeErrors, optional(boolean())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.query('summarizeErrors', mapped.summarizeErrors);
    req.json(mapped.body);
    req.appendTemplatePath`/Overpayments/${mapped.overpaymentID}/Allocations`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(allocationsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param overpaymentID  Unique identifier for a Overpayment
   * @return Response from the API call
   */
  async getOverpaymentHistory(
    xeroTenantId: string,
    overpaymentID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoryRecords>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      overpaymentID: [overpaymentID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/Overpayments/${mapped.overpaymentID}/History`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(historyRecordsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param overpaymentID  Unique identifier for a Overpayment
   * @param body           HistoryRecords containing an array of HistoryRecord objects in
   *                                                body of request
   * @return Response from the API call
   */
  async createOverpaymentHistory(
    xeroTenantId: string,
    overpaymentID: string,
    body: HistoryRecords,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoryRecords>> {
    const req = this.createRequest('PUT');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      overpaymentID: [overpaymentID, string()],
      body: [body, historyRecordsSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/Overpayments/${mapped.overpaymentID}/History`;
    req.throwOn(
      400,
      CustomError,
      'A failed request due to validation error - API is not able to create HistoryRecord for Overpayments'
    );
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(historyRecordsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @return Response from the API call
   */
  async getPaymentServices(
    xeroTenantId: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<PaymentServices>> {
    const req = this.createRequest('GET', '/PaymentServices');
    const mapped = req.prepareArgs({ xeroTenantId: [xeroTenantId, string()] });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(paymentServicesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param body           PaymentServices array with PaymentService object in body of
   *                                                 request
   * @return Response from the API call
   */
  async createPaymentService(
    xeroTenantId: string,
    body: PaymentServices,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<PaymentServices>> {
    const req = this.createRequest('PUT', '/PaymentServices');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      body: [body, paymentServicesSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(paymentServicesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId      Xero identifier for Tenant
   * @param ifModifiedSince   Only records created or modified since this timestamp will be returned
   * @param where             Filter by an any element
   * @param order             Order by an any element
   * @param page              Up to 100 payments will be returned in a single API call
   * @return Response from the API call
   */
  async getPayments(
    xeroTenantId: string,
    ifModifiedSince?: string,
    where?: string,
    order?: string,
    page?: number,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Payments>> {
    const req = this.createRequest('GET', '/Payments');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      ifModifiedSince: [ifModifiedSince, optional(string())],
      where: [where, optional(string())],
      order: [order, optional(string())],
      page: [page, optional(number())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('If-Modified-Since', mapped.ifModifiedSince);
    req.query('where', mapped.where);
    req.query('order', mapped.order);
    req.query('page', mapped.page);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(paymentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param body           Request body with a single Payment object
   * @return Response from the API call
   */
  async createPayment(
    xeroTenantId: string,
    body: Payment,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Payments>> {
    const req = this.createRequest('POST', '/Payments');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      body: [body, paymentSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(paymentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param body            Payments array with Payment object in body of request
   * @param summarizeErrors If false return 200 OK and mix of successfully created objects and any
   *                                           with validation errors
   * @return Response from the API call
   */
  async createPayments(
    xeroTenantId: string,
    body: Payments,
    summarizeErrors?: boolean,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Payments>> {
    const req = this.createRequest('PUT', '/Payments');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      body: [body, paymentsSchema],
      summarizeErrors: [summarizeErrors, optional(boolean())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.query('summarizeErrors', mapped.summarizeErrors);
    req.json(mapped.body);
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(paymentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param paymentID      Unique identifier for a Payment
   * @return Response from the API call
   */
  async getPayment(
    xeroTenantId: string,
    paymentID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Payments>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      paymentID: [paymentID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/Payments/${mapped.paymentID}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(paymentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param paymentID      Unique identifier for a Payment
   * @param body
   * @return Response from the API call
   */
  async deletePayment(
    xeroTenantId: string,
    paymentID: string,
    body: PaymentDelete,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Payments>> {
    const req = this.createRequest('POST');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      paymentID: [paymentID, string()],
      body: [body, paymentDeleteSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/Payments/${mapped.paymentID}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(paymentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param paymentID      Unique identifier for a Payment
   * @return Response from the API call
   */
  async getPaymentHistory(
    xeroTenantId: string,
    paymentID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoryRecords>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      paymentID: [paymentID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/Payments/${mapped.paymentID}/History`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(historyRecordsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param paymentID      Unique identifier for a Payment
   * @param body           HistoryRecords containing an array of HistoryRecord objects in
   *                                                body of request
   * @return Response from the API call
   */
  async createPaymentHistory(
    xeroTenantId: string,
    paymentID: string,
    body: HistoryRecords,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoryRecords>> {
    const req = this.createRequest('PUT');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      paymentID: [paymentID, string()],
      body: [body, historyRecordsSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/Payments/${mapped.paymentID}/History`;
    req.throwOn(
      400,
      CustomError,
      'A failed request due to validation error - API is not able to create HistoryRecord for Payments'
    );
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(historyRecordsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId      Xero identifier for Tenant
   * @param ifModifiedSince   Only records created or modified since this timestamp will be returned
   * @param where             Filter by an any element
   * @param order             Order by an any element
   * @param page              e.g. page=1 – Up to 100 prepayments will be returned in a single API call with
   *                                    line items shown for each overpayment
   * @param unitdp            e.g. unitdp=4 – (Unit Decimal Places) You can opt in to use four decimal
   *                                    places for unit amounts
   * @return Response from the API call
   */
  async getPrepayments(
    xeroTenantId: string,
    ifModifiedSince?: string,
    where?: string,
    order?: string,
    page?: number,
    unitdp?: number,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Prepayments>> {
    const req = this.createRequest('GET', '/Prepayments');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      ifModifiedSince: [ifModifiedSince, optional(string())],
      where: [where, optional(string())],
      order: [order, optional(string())],
      page: [page, optional(number())],
      unitdp: [unitdp, optional(number())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('If-Modified-Since', mapped.ifModifiedSince);
    req.query('where', mapped.where);
    req.query('order', mapped.order);
    req.query('page', mapped.page);
    req.query('unitdp', mapped.unitdp);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(prepaymentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param prepaymentID   Unique identifier for a PrePayment
   * @return Response from the API call
   */
  async getPrepayment(
    xeroTenantId: string,
    prepaymentID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Prepayments>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      prepaymentID: [prepaymentID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/Prepayments/${mapped.prepaymentID}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(prepaymentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param prepaymentID    Unique identifier for Prepayment
   * @param body            Allocations with an array of Allocation object in body of request
   * @param summarizeErrors If false return 200 OK and mix of successfully created objects and
   *                                              any with validation errors
   * @return Response from the API call
   */
  async createPrepaymentAllocations(
    xeroTenantId: string,
    prepaymentID: string,
    body: Allocations,
    summarizeErrors?: boolean,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Allocations>> {
    const req = this.createRequest('PUT');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      prepaymentID: [prepaymentID, string()],
      body: [body, allocationsSchema],
      summarizeErrors: [summarizeErrors, optional(boolean())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.query('summarizeErrors', mapped.summarizeErrors);
    req.json(mapped.body);
    req.appendTemplatePath`/Prepayments/${mapped.prepaymentID}/Allocations`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(allocationsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param prepaymentID   Unique identifier for a PrePayment
   * @return Response from the API call
   */
  async getPrepaymentHistory(
    xeroTenantId: string,
    prepaymentID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoryRecords>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      prepaymentID: [prepaymentID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/Prepayments/${mapped.prepaymentID}/History`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(historyRecordsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param prepaymentID   Unique identifier for a PrePayment
   * @param body           HistoryRecords containing an array of HistoryRecord objects in
   *                                                body of request
   * @return Response from the API call
   */
  async createPrepaymentHistory(
    xeroTenantId: string,
    prepaymentID: string,
    body: HistoryRecords,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoryRecords>> {
    const req = this.createRequest('PUT');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      prepaymentID: [prepaymentID, string()],
      body: [body, historyRecordsSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/Prepayments/${mapped.prepaymentID}/History`;
    req.throwOn(
      400,
      CustomError,
      'Unsupported - return response incorrect exception, API is not able to create HistoryRecord for Expense Claims'
    );
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(historyRecordsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId      Xero identifier for Tenant
   * @param ifModifiedSince   Only records created or modified since this timestamp will be returned
   * @param status            Filter by purchase order status
   * @param dateFrom          Filter by purchase order date (e.g. GET https://.../PurchaseOrders?
   *                                          DateFrom=2015-12-01&DateTo=2015-12-31
   * @param dateTo            Filter by purchase order date (e.g. GET https://.../PurchaseOrders?
   *                                          DateFrom=2015-12-01&DateTo=2015-12-31
   * @param order             Order by an any element
   * @param page              To specify a page, append the page parameter to the URL e.g. ?page=1. If
   *                                          there are 100 records in the response you will need to check if there is
   *                                          any more data by fetching the next page e.g ?page=2 and continuing this
   *                                          process until no more results are returned.
   * @return Response from the API call
   */
  async getPurchaseOrders(
    xeroTenantId: string,
    ifModifiedSince?: string,
    status?: Status20Enum,
    dateFrom?: string,
    dateTo?: string,
    order?: string,
    page?: number,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<PurchaseOrders>> {
    const req = this.createRequest('GET', '/PurchaseOrders');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      ifModifiedSince: [ifModifiedSince, optional(string())],
      status: [status, optional(status20EnumSchema)],
      dateFrom: [dateFrom, optional(string())],
      dateTo: [dateTo, optional(string())],
      order: [order, optional(string())],
      page: [page, optional(number())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('If-Modified-Since', mapped.ifModifiedSince);
    req.query('Status', mapped.status);
    req.query('DateFrom', mapped.dateFrom);
    req.query('DateTo', mapped.dateTo);
    req.query('order', mapped.order);
    req.query('page', mapped.page);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(purchaseOrdersSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param body
   * @param summarizeErrors If false return 200 OK and mix of successfully created objects
   *                                                 and any with validation errors
   * @return Response from the API call
   */
  async updateOrCreatePurchaseOrders(
    xeroTenantId: string,
    body: PurchaseOrders,
    summarizeErrors?: boolean,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<PurchaseOrders>> {
    const req = this.createRequest('POST', '/PurchaseOrders');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      body: [body, purchaseOrdersSchema],
      summarizeErrors: [summarizeErrors, optional(boolean())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.query('summarizeErrors', mapped.summarizeErrors);
    req.json(mapped.body);
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(purchaseOrdersSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param body            PurchaseOrders with an array of PurchaseOrder object in body of
   *                                                 request
   * @param summarizeErrors If false return 200 OK and mix of successfully created objects
   *                                                 and any with validation errors
   * @return Response from the API call
   */
  async createPurchaseOrders(
    xeroTenantId: string,
    body: PurchaseOrders,
    summarizeErrors?: boolean,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<PurchaseOrders>> {
    const req = this.createRequest('PUT', '/PurchaseOrders');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      body: [body, purchaseOrdersSchema],
      summarizeErrors: [summarizeErrors, optional(boolean())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.query('summarizeErrors', mapped.summarizeErrors);
    req.json(mapped.body);
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(purchaseOrdersSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param purchaseOrderID Unique identifier for a PurchaseOrder
   * @return Response from the API call
   */
  async getPurchaseOrder(
    xeroTenantId: string,
    purchaseOrderID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<PurchaseOrders>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      purchaseOrderID: [purchaseOrderID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/PurchaseOrders/${mapped.purchaseOrderID}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(purchaseOrdersSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param purchaseOrderID Unique identifier for a PurchaseOrder
   * @param body
   * @return Response from the API call
   */
  async updatePurchaseOrder(
    xeroTenantId: string,
    purchaseOrderID: string,
    body: PurchaseOrders,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<PurchaseOrders>> {
    const req = this.createRequest('POST');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      purchaseOrderID: [purchaseOrderID, string()],
      body: [body, purchaseOrdersSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/PurchaseOrders/${mapped.purchaseOrderID}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(purchaseOrdersSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param purchaseOrderID Unique identifier for Purchase Orders object
   * @return Response from the API call
   */
  async getPurchaseOrderAttachments(
    xeroTenantId: string,
    purchaseOrderID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Attachments>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      purchaseOrderID: [purchaseOrderID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/PurchaseOrders/${mapped.purchaseOrderID}/Attachments`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(attachmentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param purchaseOrderID Unique identifier for Purchase Order object
   * @param attachmentID    Unique identifier for Attachment object
   * @param contentType     The mime type of the attachment file you are retrieving i.e image/jpg,
   *                                  application/pdf
   * @return Response from the API call
   */
  async getPurchaseOrderAttachmentById(
    xeroTenantId: string,
    purchaseOrderID: string,
    attachmentID: string,
    contentType: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<NodeJS.ReadableStream | Blob>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      purchaseOrderID: [purchaseOrderID, string()],
      attachmentID: [attachmentID, string()],
      contentType: [contentType, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('contentType', mapped.contentType);
    req.appendTemplatePath`/PurchaseOrders/${mapped.purchaseOrderID}/Attachments/${mapped.attachmentID}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsStream(requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param purchaseOrderID Unique identifier for Purchase Order object
   * @param fileName        Name of the attachment
   * @param contentType     The mime type of the attachment file you are retrieving i.e image/jpg,
   *                                  application/pdf
   * @return Response from the API call
   */
  async getPurchaseOrderAttachmentByFileName(
    xeroTenantId: string,
    purchaseOrderID: string,
    fileName: string,
    contentType: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<NodeJS.ReadableStream | Blob>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      purchaseOrderID: [purchaseOrderID, string()],
      fileName: [fileName, string()],
      contentType: [contentType, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('contentType', mapped.contentType);
    req.appendTemplatePath`/PurchaseOrders/${mapped.purchaseOrderID}/Attachments/${mapped.fileName}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsStream(requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param purchaseOrderID Unique identifier for Purchase Order object
   * @param fileName        Name of the attachment
   * @param body            Byte array of file in body of request
   * @return Response from the API call
   */
  async updatePurchaseOrderAttachmentByFileName(
    xeroTenantId: string,
    purchaseOrderID: string,
    fileName: string,
    body: FileWrapper,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Attachments>> {
    const req = this.createRequest('POST');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      purchaseOrderID: [purchaseOrderID, string()],
      fileName: [fileName, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/octet-stream');
    req.formData({ body: body });
    req.appendTemplatePath`/PurchaseOrders/${mapped.purchaseOrderID}/Attachments/${mapped.fileName}`;
    req.throwOn(
      400,
      CustomError,
      'Validation Error - some data was incorrect returns response of type Error'
    );
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(attachmentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param purchaseOrderID Unique identifier for Purchase Order object
   * @param fileName        Name of the attachment
   * @param body            Byte array of file in body of request
   * @return Response from the API call
   */
  async createPurchaseOrderAttachmentByFileName(
    xeroTenantId: string,
    purchaseOrderID: string,
    fileName: string,
    body: FileWrapper,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Attachments>> {
    const req = this.createRequest('PUT');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      purchaseOrderID: [purchaseOrderID, string()],
      fileName: [fileName, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/octet-stream');
    req.formData({ body: body });
    req.appendTemplatePath`/PurchaseOrders/${mapped.purchaseOrderID}/Attachments/${mapped.fileName}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(attachmentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param purchaseOrderID Unique identifier for a PurchaseOrder
   * @return Response from the API call
   */
  async getPurchaseOrderHistory(
    xeroTenantId: string,
    purchaseOrderID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoryRecords>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      purchaseOrderID: [purchaseOrderID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/PurchaseOrders/${mapped.purchaseOrderID}/History`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(historyRecordsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param purchaseOrderID Unique identifier for a PurchaseOrder
   * @param body            HistoryRecords containing an array of HistoryRecord objects in
   *                                                 body of request
   * @return Response from the API call
   */
  async createPurchaseOrderHistory(
    xeroTenantId: string,
    purchaseOrderID: string,
    body: HistoryRecords,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoryRecords>> {
    const req = this.createRequest('PUT');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      purchaseOrderID: [purchaseOrderID, string()],
      body: [body, historyRecordsSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/PurchaseOrders/${mapped.purchaseOrderID}/History`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(historyRecordsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param purchaseOrderID Unique identifier for an Purchase Order
   * @return Response from the API call
   */
  async getPurchaseOrderAsPdf(
    xeroTenantId: string,
    purchaseOrderID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<unknown>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      purchaseOrderID: [purchaseOrderID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/PurchaseOrders/${mapped.purchaseOrderID}/pdf`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(unknown(), requestOptions);
  }

  /**
   * @param xeroTenantId        Xero identifier for Tenant
   * @param purchaseOrderNumber Unique identifier for a PurchaseOrder
   * @return Response from the API call
   */
  async getPurchaseOrderByNumber(
    xeroTenantId: string,
    purchaseOrderNumber: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<PurchaseOrders>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      purchaseOrderNumber: [purchaseOrderNumber, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/PurchaseOrders/${mapped.purchaseOrderNumber}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(purchaseOrdersSchema, requestOptions);
  }

  /**
   * @param xeroTenantId      Xero identifier for Tenant
   * @param ifModifiedSince   Only records created or modified since this timestamp will be returned
   * @param dateFrom          Filter for quotes after a particular date
   * @param dateTo            Filter for quotes before a particular date
   * @param expiryDateFrom    Filter for quotes expiring after a particular date
   * @param expiryDateTo      Filter for quotes before a particular date
   * @param contactID         Filter for quotes belonging to a particular contact
   * @param status            Filter for quotes of a particular Status
   * @param page              e.g. page=1 – Up to 100 Quotes will be returned in a single API call with line
   *                                    items shown for each quote
   * @param order             Order by an any element
   * @param quoteNumber       Filter by quote number (e.g. GET https://.../Quotes?QuoteNumber=QU-0001)
   * @return Response from the API call
   */
  async getQuotes(
    xeroTenantId: string,
    ifModifiedSince?: string,
    dateFrom?: string,
    dateTo?: string,
    expiryDateFrom?: string,
    expiryDateTo?: string,
    contactID?: string,
    status?: string,
    page?: number,
    order?: string,
    quoteNumber?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Quotes>> {
    const req = this.createRequest('GET', '/Quotes');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      ifModifiedSince: [ifModifiedSince, optional(string())],
      dateFrom: [dateFrom, optional(string())],
      dateTo: [dateTo, optional(string())],
      expiryDateFrom: [expiryDateFrom, optional(string())],
      expiryDateTo: [expiryDateTo, optional(string())],
      contactID: [contactID, optional(string())],
      status: [status, optional(string())],
      page: [page, optional(number())],
      order: [order, optional(string())],
      quoteNumber: [quoteNumber, optional(string())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('If-Modified-Since', mapped.ifModifiedSince);
    req.query('DateFrom', mapped.dateFrom);
    req.query('DateTo', mapped.dateTo);
    req.query('ExpiryDateFrom', mapped.expiryDateFrom);
    req.query('ExpiryDateTo', mapped.expiryDateTo);
    req.query('ContactID', mapped.contactID);
    req.query('Status', mapped.status);
    req.query('page', mapped.page);
    req.query('order', mapped.order);
    req.query('QuoteNumber', mapped.quoteNumber);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(quotesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param body
   * @param summarizeErrors If false return 200 OK and mix of successfully created objects and any
   *                                         with validation errors
   * @return Response from the API call
   */
  async updateOrCreateQuotes(
    xeroTenantId: string,
    body: Quotes,
    summarizeErrors?: boolean,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Quotes>> {
    const req = this.createRequest('POST', '/Quotes');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      body: [body, quotesSchema],
      summarizeErrors: [summarizeErrors, optional(boolean())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.query('summarizeErrors', mapped.summarizeErrors);
    req.json(mapped.body);
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(quotesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param body            Quotes with an array of Quote object in body of request
   * @param summarizeErrors If false return 200 OK and mix of successfully created objects and any
   *                                         with validation errors
   * @return Response from the API call
   */
  async createQuotes(
    xeroTenantId: string,
    body: Quotes,
    summarizeErrors?: boolean,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Quotes>> {
    const req = this.createRequest('PUT', '/Quotes');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      body: [body, quotesSchema],
      summarizeErrors: [summarizeErrors, optional(boolean())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.query('summarizeErrors', mapped.summarizeErrors);
    req.json(mapped.body);
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(quotesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param quoteID        Unique identifier for an Quote
   * @return Response from the API call
   */
  async getQuote(
    xeroTenantId: string,
    quoteID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Quotes>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      quoteID: [quoteID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/Quotes/${mapped.quoteID}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(quotesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param quoteID        Unique identifier for an Quote
   * @param body
   * @return Response from the API call
   */
  async updateQuote(
    xeroTenantId: string,
    quoteID: string,
    body: Quotes,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Quotes>> {
    const req = this.createRequest('POST');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      quoteID: [quoteID, string()],
      body: [body, quotesSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/Quotes/${mapped.quoteID}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(quotesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param quoteID        Unique identifier for Quote object
   * @return Response from the API call
   */
  async getQuoteAttachments(
    xeroTenantId: string,
    quoteID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Attachments>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      quoteID: [quoteID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/Quotes/${mapped.quoteID}/Attachments`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(attachmentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param quoteID        Unique identifier for Quote object
   * @param attachmentID   Unique identifier for Attachment object
   * @param contentType    The mime type of the attachment file you are retrieving i.e image/jpg,
   *                                 application/pdf
   * @return Response from the API call
   */
  async getQuoteAttachmentById(
    xeroTenantId: string,
    quoteID: string,
    attachmentID: string,
    contentType: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<NodeJS.ReadableStream | Blob>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      quoteID: [quoteID, string()],
      attachmentID: [attachmentID, string()],
      contentType: [contentType, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('contentType', mapped.contentType);
    req.appendTemplatePath`/Quotes/${mapped.quoteID}/Attachments/${mapped.attachmentID}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsStream(requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param quoteID        Unique identifier for Quote object
   * @param fileName       Name of the attachment
   * @param contentType    The mime type of the attachment file you are retrieving i.e image/jpg,
   *                                 application/pdf
   * @return Response from the API call
   */
  async getQuoteAttachmentByFileName(
    xeroTenantId: string,
    quoteID: string,
    fileName: string,
    contentType: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<NodeJS.ReadableStream | Blob>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      quoteID: [quoteID, string()],
      fileName: [fileName, string()],
      contentType: [contentType, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('contentType', mapped.contentType);
    req.appendTemplatePath`/Quotes/${mapped.quoteID}/Attachments/${mapped.fileName}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsStream(requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param quoteID        Unique identifier for Quote object
   * @param fileName       Name of the attachment
   * @param body           Byte array of file in body of request
   * @return Response from the API call
   */
  async updateQuoteAttachmentByFileName(
    xeroTenantId: string,
    quoteID: string,
    fileName: string,
    body: FileWrapper,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Attachments>> {
    const req = this.createRequest('POST');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      quoteID: [quoteID, string()],
      fileName: [fileName, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/octet-stream');
    req.formData({ body: body });
    req.appendTemplatePath`/Quotes/${mapped.quoteID}/Attachments/${mapped.fileName}`;
    req.throwOn(
      400,
      CustomError,
      'Validation Error - some data was incorrect returns response of type Error'
    );
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(attachmentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param quoteID        Unique identifier for Quote object
   * @param fileName       Name of the attachment
   * @param body           Byte array of file in body of request
   * @return Response from the API call
   */
  async createQuoteAttachmentByFileName(
    xeroTenantId: string,
    quoteID: string,
    fileName: string,
    body: FileWrapper,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Attachments>> {
    const req = this.createRequest('PUT');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      quoteID: [quoteID, string()],
      fileName: [fileName, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/octet-stream');
    req.formData({ body: body });
    req.appendTemplatePath`/Quotes/${mapped.quoteID}/Attachments/${mapped.fileName}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(attachmentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param quoteID        Unique identifier for an Quote
   * @return Response from the API call
   */
  async getQuoteHistory(
    xeroTenantId: string,
    quoteID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoryRecords>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      quoteID: [quoteID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/Quotes/${mapped.quoteID}/History`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(historyRecordsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param quoteID        Unique identifier for an Quote
   * @param body           HistoryRecords containing an array of HistoryRecord objects in
   *                                                body of request
   * @return Response from the API call
   */
  async createQuoteHistory(
    xeroTenantId: string,
    quoteID: string,
    body: HistoryRecords,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoryRecords>> {
    const req = this.createRequest('PUT');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      quoteID: [quoteID, string()],
      body: [body, historyRecordsSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/Quotes/${mapped.quoteID}/History`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(historyRecordsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param quoteID        Unique identifier for an Quote
   * @return Response from the API call
   */
  async getQuoteAsPdf(
    xeroTenantId: string,
    quoteID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<unknown>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      quoteID: [quoteID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/Quotes/${mapped.quoteID}/pdf`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(unknown(), requestOptions);
  }

  /**
   * @param xeroTenantId      Xero identifier for Tenant
   * @param ifModifiedSince   Only records created or modified since this timestamp will be returned
   * @param where             Filter by an any element
   * @param order             Order by an any element
   * @param unitdp            e.g. unitdp=4 – (Unit Decimal Places) You can opt in to use four decimal
   *                                    places for unit amounts
   * @return Response from the API call
   */
  async getReceipts(
    xeroTenantId: string,
    ifModifiedSince?: string,
    where?: string,
    order?: string,
    unitdp?: number,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Receipts>> {
    const req = this.createRequest('GET', '/Receipts');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      ifModifiedSince: [ifModifiedSince, optional(string())],
      where: [where, optional(string())],
      order: [order, optional(string())],
      unitdp: [unitdp, optional(number())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('If-Modified-Since', mapped.ifModifiedSince);
    req.query('where', mapped.where);
    req.query('order', mapped.order);
    req.query('unitdp', mapped.unitdp);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(receiptsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param body           Receipts with an array of Receipt object in body of request
   * @param unitdp         e.g. unitdp=4 – (Unit Decimal Places) You can opt in to use four decimal
   *                                          places for unit amounts
   * @return Response from the API call
   */
  async createReceipt(
    xeroTenantId: string,
    body: Receipts,
    unitdp?: number,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Receipts>> {
    const req = this.createRequest('PUT', '/Receipts');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      body: [body, receiptsSchema],
      unitdp: [unitdp, optional(number())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.query('unitdp', mapped.unitdp);
    req.json(mapped.body);
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(receiptsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param receiptID      Unique identifier for a Receipt
   * @param unitdp         e.g. unitdp=4 – (Unit Decimal Places) You can opt in to use four decimal places
   *                                 for unit amounts
   * @return Response from the API call
   */
  async getReceipt(
    xeroTenantId: string,
    receiptID: string,
    unitdp?: number,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Receipts>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      receiptID: [receiptID, string()],
      unitdp: [unitdp, optional(number())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.query('unitdp', mapped.unitdp);
    req.appendTemplatePath`/Receipts/${mapped.receiptID}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(receiptsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param receiptID      Unique identifier for a Receipt
   * @param body
   * @param unitdp         e.g. unitdp=4 – (Unit Decimal Places) You can opt in to use four decimal
   *                                          places for unit amounts
   * @return Response from the API call
   */
  async updateReceipt(
    xeroTenantId: string,
    receiptID: string,
    body: Receipts,
    unitdp?: number,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Receipts>> {
    const req = this.createRequest('POST');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      receiptID: [receiptID, string()],
      body: [body, receiptsSchema],
      unitdp: [unitdp, optional(number())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.query('unitdp', mapped.unitdp);
    req.json(mapped.body);
    req.appendTemplatePath`/Receipts/${mapped.receiptID}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(receiptsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param receiptID      Unique identifier for a Receipt
   * @return Response from the API call
   */
  async getReceiptAttachments(
    xeroTenantId: string,
    receiptID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Attachments>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      receiptID: [receiptID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/Receipts/${mapped.receiptID}/Attachments`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(attachmentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param receiptID      Unique identifier for a Receipt
   * @param attachmentID   Unique identifier for a Attachment
   * @param contentType    The mime type of the attachment file you are retrieving i.e image/jpg,
   *                                 application/pdf
   * @return Response from the API call
   */
  async getReceiptAttachmentById(
    xeroTenantId: string,
    receiptID: string,
    attachmentID: string,
    contentType: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<NodeJS.ReadableStream | Blob>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      receiptID: [receiptID, string()],
      attachmentID: [attachmentID, string()],
      contentType: [contentType, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('contentType', mapped.contentType);
    req.appendTemplatePath`/Receipts/${mapped.receiptID}/Attachments/${mapped.attachmentID}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsStream(requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param receiptID      Unique identifier for a Receipt
   * @param fileName       The name of the file being attached to the Receipt
   * @param contentType    The mime type of the attachment file you are retrieving i.e image/jpg,
   *                                 application/pdf
   * @return Response from the API call
   */
  async getReceiptAttachmentByFileName(
    xeroTenantId: string,
    receiptID: string,
    fileName: string,
    contentType: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<NodeJS.ReadableStream | Blob>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      receiptID: [receiptID, string()],
      fileName: [fileName, string()],
      contentType: [contentType, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('contentType', mapped.contentType);
    req.appendTemplatePath`/Receipts/${mapped.receiptID}/Attachments/${mapped.fileName}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsStream(requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param receiptID      Unique identifier for a Receipt
   * @param fileName       The name of the file being attached to the Receipt
   * @param body           Byte array of file in body of request
   * @return Response from the API call
   */
  async updateReceiptAttachmentByFileName(
    xeroTenantId: string,
    receiptID: string,
    fileName: string,
    body: FileWrapper,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Attachments>> {
    const req = this.createRequest('POST');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      receiptID: [receiptID, string()],
      fileName: [fileName, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/octet-stream');
    req.formData({ body: body });
    req.appendTemplatePath`/Receipts/${mapped.receiptID}/Attachments/${mapped.fileName}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(attachmentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param receiptID      Unique identifier for a Receipt
   * @param fileName       The name of the file being attached to the Receipt
   * @param body           Byte array of file in body of request
   * @return Response from the API call
   */
  async createReceiptAttachmentByFileName(
    xeroTenantId: string,
    receiptID: string,
    fileName: string,
    body: FileWrapper,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Attachments>> {
    const req = this.createRequest('PUT');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      receiptID: [receiptID, string()],
      fileName: [fileName, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/octet-stream');
    req.formData({ body: body });
    req.appendTemplatePath`/Receipts/${mapped.receiptID}/Attachments/${mapped.fileName}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(attachmentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param receiptID      Unique identifier for a Receipt
   * @return Response from the API call
   */
  async getReceiptHistory(
    xeroTenantId: string,
    receiptID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoryRecords>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      receiptID: [receiptID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/Receipts/${mapped.receiptID}/History`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(historyRecordsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param receiptID      Unique identifier for a Receipt
   * @param body           HistoryRecords containing an array of HistoryRecord objects in
   *                                                body of request
   * @return Response from the API call
   */
  async createReceiptHistory(
    xeroTenantId: string,
    receiptID: string,
    body: HistoryRecords,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoryRecords>> {
    const req = this.createRequest('PUT');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      receiptID: [receiptID, string()],
      body: [body, historyRecordsSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/Receipts/${mapped.receiptID}/History`;
    req.throwOn(
      400,
      CustomError,
      'Unsupported - return response incorrect exception, API is not able to create HistoryRecord for Receipts'
    );
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(historyRecordsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param where          Filter by an any element
   * @param order          Order by an any element
   * @return Response from the API call
   */
  async getRepeatingInvoices(
    xeroTenantId: string,
    where?: string,
    order?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<RepeatingInvoices>> {
    const req = this.createRequest('GET', '/RepeatingInvoices');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      where: [where, optional(string())],
      order: [order, optional(string())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.query('where', mapped.where);
    req.query('order', mapped.order);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(repeatingInvoicesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId       Xero identifier for Tenant
   * @param repeatingInvoiceID Unique identifier for a Repeating Invoice
   * @return Response from the API call
   */
  async getRepeatingInvoice(
    xeroTenantId: string,
    repeatingInvoiceID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<RepeatingInvoices>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      repeatingInvoiceID: [repeatingInvoiceID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/RepeatingInvoices/${mapped.repeatingInvoiceID}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(repeatingInvoicesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId       Xero identifier for Tenant
   * @param repeatingInvoiceID Unique identifier for a Repeating Invoice
   * @return Response from the API call
   */
  async getRepeatingInvoiceAttachments(
    xeroTenantId: string,
    repeatingInvoiceID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Attachments>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      repeatingInvoiceID: [repeatingInvoiceID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/RepeatingInvoices/${mapped.repeatingInvoiceID}/Attachments`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(attachmentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId       Xero identifier for Tenant
   * @param repeatingInvoiceID Unique identifier for a Repeating Invoice
   * @param attachmentID       Unique identifier for a Attachment
   * @param contentType        The mime type of the attachment file you are retrieving i.e image/jpg,
   *                                     application/pdf
   * @return Response from the API call
   */
  async getRepeatingInvoiceAttachmentById(
    xeroTenantId: string,
    repeatingInvoiceID: string,
    attachmentID: string,
    contentType: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<NodeJS.ReadableStream | Blob>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      repeatingInvoiceID: [repeatingInvoiceID, string()],
      attachmentID: [attachmentID, string()],
      contentType: [contentType, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('contentType', mapped.contentType);
    req.appendTemplatePath`/RepeatingInvoices/${mapped.repeatingInvoiceID}/Attachments/${mapped.attachmentID}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsStream(requestOptions);
  }

  /**
   * @param xeroTenantId       Xero identifier for Tenant
   * @param repeatingInvoiceID Unique identifier for a Repeating Invoice
   * @param fileName           The name of the file being attached to a Repeating Invoice
   * @param contentType        The mime type of the attachment file you are retrieving i.e image/jpg,
   *                                     application/pdf
   * @return Response from the API call
   */
  async getRepeatingInvoiceAttachmentByFileName(
    xeroTenantId: string,
    repeatingInvoiceID: string,
    fileName: string,
    contentType: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<NodeJS.ReadableStream | Blob>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      repeatingInvoiceID: [repeatingInvoiceID, string()],
      fileName: [fileName, string()],
      contentType: [contentType, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('contentType', mapped.contentType);
    req.appendTemplatePath`/RepeatingInvoices/${mapped.repeatingInvoiceID}/Attachments/${mapped.fileName}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsStream(requestOptions);
  }

  /**
   * @param xeroTenantId       Xero identifier for Tenant
   * @param repeatingInvoiceID Unique identifier for a Repeating Invoice
   * @param fileName           The name of the file being attached to a Repeating Invoice
   * @param body               Byte array of file in body of request
   * @return Response from the API call
   */
  async updateRepeatingInvoiceAttachmentByFileName(
    xeroTenantId: string,
    repeatingInvoiceID: string,
    fileName: string,
    body: FileWrapper,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Attachments>> {
    const req = this.createRequest('POST');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      repeatingInvoiceID: [repeatingInvoiceID, string()],
      fileName: [fileName, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/octet-stream');
    req.formData({ body: body });
    req.appendTemplatePath`/RepeatingInvoices/${mapped.repeatingInvoiceID}/Attachments/${mapped.fileName}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(attachmentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId       Xero identifier for Tenant
   * @param repeatingInvoiceID Unique identifier for a Repeating Invoice
   * @param fileName           The name of the file being attached to a Repeating Invoice
   * @param body               Byte array of file in body of request
   * @return Response from the API call
   */
  async createRepeatingInvoiceAttachmentByFileName(
    xeroTenantId: string,
    repeatingInvoiceID: string,
    fileName: string,
    body: FileWrapper,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Attachments>> {
    const req = this.createRequest('PUT');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      repeatingInvoiceID: [repeatingInvoiceID, string()],
      fileName: [fileName, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/octet-stream');
    req.formData({ body: body });
    req.appendTemplatePath`/RepeatingInvoices/${mapped.repeatingInvoiceID}/Attachments/${mapped.fileName}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(attachmentsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId       Xero identifier for Tenant
   * @param repeatingInvoiceID Unique identifier for a Repeating Invoice
   * @return Response from the API call
   */
  async getRepeatingInvoiceHistory(
    xeroTenantId: string,
    repeatingInvoiceID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoryRecords>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      repeatingInvoiceID: [repeatingInvoiceID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/RepeatingInvoices/${mapped.repeatingInvoiceID}/History`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(historyRecordsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId       Xero identifier for Tenant
   * @param repeatingInvoiceID Unique identifier for a Repeating Invoice
   * @param body               HistoryRecords containing an array of HistoryRecord objects in
   *                                                    body of request
   * @return Response from the API call
   */
  async createRepeatingInvoiceHistory(
    xeroTenantId: string,
    repeatingInvoiceID: string,
    body: HistoryRecords,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<HistoryRecords>> {
    const req = this.createRequest('PUT');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      repeatingInvoiceID: [repeatingInvoiceID, string()],
      body: [body, historyRecordsSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/RepeatingInvoices/${mapped.repeatingInvoiceID}/History`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(historyRecordsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @return Response from the API call
   */
  async getReportBASorGSTList(
    xeroTenantId: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ReportWithRows>> {
    const req = this.createRequest('GET', '/Reports');
    const mapped = req.prepareArgs({ xeroTenantId: [xeroTenantId, string()] });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(reportWithRowsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param contactId      Unique identifier for a Contact
   * @param date           The date of the Aged Payables By Contact report
   * @param fromDate       The from date of the Aged Payables By Contact report
   * @param toDate         The to date of the Aged Payables By Contact report
   * @return Response from the API call
   */
  async getReportAgedPayablesByContact(
    xeroTenantId: string,
    contactId: string,
    date?: string,
    fromDate?: string,
    toDate?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ReportWithRows>> {
    const req = this.createRequest('GET', '/Reports/AgedPayablesByContact');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      contactId: [contactId, string()],
      date: [date, optional(string())],
      fromDate: [fromDate, optional(string())],
      toDate: [toDate, optional(string())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.query('contactId', mapped.contactId);
    req.query('date', mapped.date);
    req.query('fromDate', mapped.fromDate);
    req.query('toDate', mapped.toDate);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(reportWithRowsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param contactId      Unique identifier for a Contact
   * @param date           The date of the Aged Receivables By Contact report
   * @param fromDate       The from date of the Aged Receivables By Contact report
   * @param toDate         The to date of the Aged Receivables By Contact report
   * @return Response from the API call
   */
  async getReportAgedReceivablesByContact(
    xeroTenantId: string,
    contactId: string,
    date?: string,
    fromDate?: string,
    toDate?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ReportWithRows>> {
    const req = this.createRequest('GET', '/Reports/AgedReceivablesByContact');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      contactId: [contactId, string()],
      date: [date, optional(string())],
      fromDate: [fromDate, optional(string())],
      toDate: [toDate, optional(string())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.query('contactId', mapped.contactId);
    req.query('date', mapped.date);
    req.query('fromDate', mapped.fromDate);
    req.query('toDate', mapped.toDate);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(reportWithRowsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId      Xero identifier for Tenant
   * @param date              The date of the Balance Sheet report
   * @param periods           The number of periods for the Balance Sheet report
   * @param timeframe         The period size to compare to (MONTH, QUARTER, YEAR)
   * @param trackingOptionID1 The tracking option 1 for the Balance Sheet report
   * @param trackingOptionID2 The tracking option 2 for the Balance Sheet report
   * @param standardLayout    The standard layout boolean for the Balance Sheet report
   * @param paymentsOnly      return a cash basis for the Balance Sheet report
   * @return Response from the API call
   */
  async getReportBalanceSheet(
    xeroTenantId: string,
    date?: string,
    periods?: number,
    timeframe?: TimeframeEnum,
    trackingOptionID1?: string,
    trackingOptionID2?: string,
    standardLayout?: boolean,
    paymentsOnly?: boolean,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ReportWithRows>> {
    const req = this.createRequest('GET', '/Reports/BalanceSheet');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      date: [date, optional(string())],
      periods: [periods, optional(number())],
      timeframe: [timeframe, optional(timeframeEnumSchema)],
      trackingOptionID1: [trackingOptionID1, optional(string())],
      trackingOptionID2: [trackingOptionID2, optional(string())],
      standardLayout: [standardLayout, optional(boolean())],
      paymentsOnly: [paymentsOnly, optional(boolean())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.query('date', mapped.date);
    req.query('periods', mapped.periods);
    req.query('timeframe', mapped.timeframe);
    req.query('trackingOptionID1', mapped.trackingOptionID1);
    req.query('trackingOptionID2', mapped.trackingOptionID2);
    req.query('standardLayout', mapped.standardLayout);
    req.query('paymentsOnly', mapped.paymentsOnly);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(reportWithRowsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param fromDate       The from date for the Bank Summary report e.g. 2018-03-31
   * @param toDate         The to date for the Bank Summary report e.g. 2018-03-31
   * @return Response from the API call
   */
  async getReportBankSummary(
    xeroTenantId: string,
    fromDate?: string,
    toDate?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ReportWithRows>> {
    const req = this.createRequest('GET', '/Reports/BankSummary');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      fromDate: [fromDate, optional(string())],
      toDate: [toDate, optional(string())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.query('fromDate', mapped.fromDate);
    req.query('toDate', mapped.toDate);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(reportWithRowsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param date           The date for the Bank Summary report e.g. 2018-03-31
   * @param period         The number of periods to compare (integer between 1 and 12)
   * @param timeframe      The period size to compare to (1=month, 3=quarter, 12=year)
   * @return Response from the API call
   */
  async getReportBudgetSummary(
    xeroTenantId: string,
    date?: string,
    period?: number,
    timeframe?: number,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ReportWithRows>> {
    const req = this.createRequest('GET', '/Reports/BudgetSummary');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      date: [date, optional(string())],
      period: [period, optional(number())],
      timeframe: [timeframe, optional(number())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.query('date', mapped.date);
    req.query('period', mapped.period);
    req.query('timeframe', mapped.timeframe);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(reportWithRowsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param date           The date for the Bank Summary report e.g. 2018-03-31
   * @return Response from the API call
   */
  async getReportExecutiveSummary(
    xeroTenantId: string,
    date?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ReportWithRows>> {
    const req = this.createRequest('GET', '/Reports/ExecutiveSummary');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      date: [date, optional(string())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.query('date', mapped.date);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(reportWithRowsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId        Xero identifier for Tenant
   * @param fromDate            The from date for the ProfitAndLoss report e.g. 2018-03-31
   * @param toDate              The to date for the ProfitAndLoss report e.g. 2018-03-31
   * @param periods             The number of periods to compare (integer between 1 and 12)
   * @param timeframe           The period size to compare to (MONTH, QUARTER, YEAR)
   * @param trackingCategoryID  The trackingCategory 1 for the ProfitAndLoss report
   * @param trackingCategoryID2 The trackingCategory 2 for the ProfitAndLoss report
   * @param trackingOptionID    The tracking option 1 for the ProfitAndLoss report
   * @param trackingOptionID2   The tracking option 2 for the ProfitAndLoss report
   * @param standardLayout      Return the standard layout for the ProfitAndLoss report
   * @param paymentsOnly        Return cash only basis for the ProfitAndLoss report
   * @return Response from the API call
   */
  async getReportProfitAndLoss(
    xeroTenantId: string,
    fromDate?: string,
    toDate?: string,
    periods?: number,
    timeframe?: TimeframeEnum,
    trackingCategoryID?: string,
    trackingCategoryID2?: string,
    trackingOptionID?: string,
    trackingOptionID2?: string,
    standardLayout?: boolean,
    paymentsOnly?: boolean,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ReportWithRows>> {
    const req = this.createRequest('GET', '/Reports/ProfitAndLoss');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      fromDate: [fromDate, optional(string())],
      toDate: [toDate, optional(string())],
      periods: [periods, optional(number())],
      timeframe: [timeframe, optional(timeframeEnumSchema)],
      trackingCategoryID: [trackingCategoryID, optional(string())],
      trackingCategoryID2: [trackingCategoryID2, optional(string())],
      trackingOptionID: [trackingOptionID, optional(string())],
      trackingOptionID2: [trackingOptionID2, optional(string())],
      standardLayout: [standardLayout, optional(boolean())],
      paymentsOnly: [paymentsOnly, optional(boolean())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.query('fromDate', mapped.fromDate);
    req.query('toDate', mapped.toDate);
    req.query('periods', mapped.periods);
    req.query('timeframe', mapped.timeframe);
    req.query('trackingCategoryID', mapped.trackingCategoryID);
    req.query('trackingCategoryID2', mapped.trackingCategoryID2);
    req.query('trackingOptionID', mapped.trackingOptionID);
    req.query('trackingOptionID2', mapped.trackingOptionID2);
    req.query('standardLayout', mapped.standardLayout);
    req.query('paymentsOnly', mapped.paymentsOnly);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(reportWithRowsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param reportYear     The year of the 1099 report
   * @return Response from the API call
   */
  async getReportTenNinetyNine(
    xeroTenantId: string,
    reportYear?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Reports>> {
    const req = this.createRequest('GET', '/Reports/TenNinetyNine');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      reportYear: [reportYear, optional(string())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.query('reportYear', mapped.reportYear);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(reportsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param date           The date for the Trial Balance report e.g. 2018-03-31
   * @param paymentsOnly   Return cash only basis for the Trial Balance report
   * @return Response from the API call
   */
  async getReportTrialBalance(
    xeroTenantId: string,
    date?: string,
    paymentsOnly?: boolean,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ReportWithRows>> {
    const req = this.createRequest('GET', '/Reports/TrialBalance');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      date: [date, optional(string())],
      paymentsOnly: [paymentsOnly, optional(boolean())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.query('date', mapped.date);
    req.query('paymentsOnly', mapped.paymentsOnly);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(reportWithRowsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param reportID       Unique identifier for a Report
   * @return Response from the API call
   */
  async getReportBASorGST(
    xeroTenantId: string,
    reportID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ReportWithRows>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      reportID: [reportID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/Reports/${mapped.reportID}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(reportWithRowsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param body           Object including an accounts array, a conversion balances array and a
   *                                       conversion date object in body of request
   * @return Response from the API call
   */
  async postSetup(
    xeroTenantId: string,
    body: Setup,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ImportSummaryObject>> {
    const req = this.createRequest('POST', '/Setup');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      body: [body, setupSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(importSummaryObjectSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param where          Filter by an any element
   * @param order          Order by an any element
   * @param taxType        Filter by tax type
   * @return Response from the API call
   */
  async getTaxRates(
    xeroTenantId: string,
    where?: string,
    order?: string,
    taxType?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<TaxRates>> {
    const req = this.createRequest('GET', '/TaxRates');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      where: [where, optional(string())],
      order: [order, optional(string())],
      taxType: [taxType, optional(string())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.query('where', mapped.where);
    req.query('order', mapped.order);
    req.query('TaxType', mapped.taxType);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(taxRatesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param body
   * @return Response from the API call
   */
  async updateTaxRate(
    xeroTenantId: string,
    body: TaxRates,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<TaxRates>> {
    const req = this.createRequest('POST', '/TaxRates');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      body: [body, taxRatesSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(taxRatesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param body           TaxRates array with TaxRate object in body of request
   * @return Response from the API call
   */
  async createTaxRates(
    xeroTenantId: string,
    body: TaxRates,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<TaxRates>> {
    const req = this.createRequest('PUT', '/TaxRates');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      body: [body, taxRatesSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(taxRatesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId    Xero identifier for Tenant
   * @param where           Filter by an any element
   * @param order           Order by an any element
   * @param includeArchived e.g. includeArchived=true - Categories and options with a status of ARCHIVED
   *                                   will be included in the response
   * @return Response from the API call
   */
  async getTrackingCategories(
    xeroTenantId: string,
    where?: string,
    order?: string,
    includeArchived?: boolean,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<TrackingCategories>> {
    const req = this.createRequest('GET', '/TrackingCategories');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      where: [where, optional(string())],
      order: [order, optional(string())],
      includeArchived: [includeArchived, optional(boolean())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.query('where', mapped.where);
    req.query('order', mapped.order);
    req.query('includeArchived', mapped.includeArchived);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(trackingCategoriesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param body           TrackingCategory object in body of request
   * @return Response from the API call
   */
  async createTrackingCategory(
    xeroTenantId: string,
    body: TrackingCategory,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<TrackingCategories>> {
    const req = this.createRequest('PUT', '/TrackingCategories');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      body: [body, trackingCategorySchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(trackingCategoriesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId       Xero identifier for Tenant
   * @param trackingCategoryID Unique identifier for a TrackingCategory
   * @return Response from the API call
   */
  async deleteTrackingCategory(
    xeroTenantId: string,
    trackingCategoryID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<TrackingCategories>> {
    const req = this.createRequest('DELETE');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      trackingCategoryID: [trackingCategoryID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/TrackingCategories/${mapped.trackingCategoryID}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(trackingCategoriesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId       Xero identifier for Tenant
   * @param trackingCategoryID Unique identifier for a TrackingCategory
   * @return Response from the API call
   */
  async getTrackingCategory(
    xeroTenantId: string,
    trackingCategoryID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<TrackingCategories>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      trackingCategoryID: [trackingCategoryID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/TrackingCategories/${mapped.trackingCategoryID}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(trackingCategoriesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId       Xero identifier for Tenant
   * @param trackingCategoryID Unique identifier for a TrackingCategory
   * @param body
   * @return Response from the API call
   */
  async updateTrackingCategory(
    xeroTenantId: string,
    trackingCategoryID: string,
    body: TrackingCategory,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<TrackingCategories>> {
    const req = this.createRequest('POST');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      trackingCategoryID: [trackingCategoryID, string()],
      body: [body, trackingCategorySchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/TrackingCategories/${mapped.trackingCategoryID}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(trackingCategoriesSchema, requestOptions);
  }

  /**
   * @param xeroTenantId       Xero identifier for Tenant
   * @param trackingCategoryID Unique identifier for a TrackingCategory
   * @param body               TrackingOption object in body of request
   * @return Response from the API call
   */
  async createTrackingOptions(
    xeroTenantId: string,
    trackingCategoryID: string,
    body: TrackingOption,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<TrackingOptions>> {
    const req = this.createRequest('PUT');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      trackingCategoryID: [trackingCategoryID, string()],
      body: [body, trackingOptionSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/TrackingCategories/${mapped.trackingCategoryID}/Options`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(trackingOptionsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId       Xero identifier for Tenant
   * @param trackingCategoryID Unique identifier for a TrackingCategory
   * @param trackingOptionID   Unique identifier for a Tracking Option
   * @return Response from the API call
   */
  async deleteTrackingOptions(
    xeroTenantId: string,
    trackingCategoryID: string,
    trackingOptionID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<TrackingOptions>> {
    const req = this.createRequest('DELETE');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      trackingCategoryID: [trackingCategoryID, string()],
      trackingOptionID: [trackingOptionID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/TrackingCategories/${mapped.trackingCategoryID}/Options/${mapped.trackingOptionID}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(trackingOptionsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId       Xero identifier for Tenant
   * @param trackingCategoryID Unique identifier for a TrackingCategory
   * @param trackingOptionID   Unique identifier for a Tracking Option
   * @param body
   * @return Response from the API call
   */
  async updateTrackingOptions(
    xeroTenantId: string,
    trackingCategoryID: string,
    trackingOptionID: string,
    body: TrackingOption,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<TrackingOptions>> {
    const req = this.createRequest('POST');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      trackingCategoryID: [trackingCategoryID, string()],
      trackingOptionID: [trackingOptionID, string()],
      body: [body, trackingOptionSchema],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.appendTemplatePath`/TrackingCategories/${mapped.trackingCategoryID}/Options/${mapped.trackingOptionID}`;
    req.throwOn(400, CustomError, 'A failed request due to validation error');
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(trackingOptionsSchema, requestOptions);
  }

  /**
   * @param xeroTenantId      Xero identifier for Tenant
   * @param ifModifiedSince   Only records created or modified since this timestamp will be returned
   * @param where             Filter by an any element
   * @param order             Order by an any element
   * @return Response from the API call
   */
  async getUsers(
    xeroTenantId: string,
    ifModifiedSince?: string,
    where?: string,
    order?: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Users>> {
    const req = this.createRequest('GET', '/Users');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      ifModifiedSince: [ifModifiedSince, optional(string())],
      where: [where, optional(string())],
      order: [order, optional(string())],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.header('If-Modified-Since', mapped.ifModifiedSince);
    req.query('where', mapped.where);
    req.query('order', mapped.order);
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(usersSchema, requestOptions);
  }

  /**
   * @param xeroTenantId   Xero identifier for Tenant
   * @param userID         Unique identifier for a User
   * @return Response from the API call
   */
  async getUser(
    xeroTenantId: string,
    userID: string,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<Users>> {
    const req = this.createRequest('GET');
    const mapped = req.prepareArgs({
      xeroTenantId: [xeroTenantId, string()],
      userID: [userID, string()],
    });
    req.header('xero-tenant-id', mapped.xeroTenantId);
    req.appendTemplatePath`/Users/${mapped.userID}`;
    req.authenticate([{ oAuth2: true }]);
    return req.callAsJson(usersSchema, requestOptions);
  }
}
